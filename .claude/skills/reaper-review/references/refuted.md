# Refuted candidates — a grep index

A row here means a reviewer raised the candidate and an independent verifier **demonstrated the
trigger cannot occur**. Nothing softer qualifies: "declined", "unreachable today" and "not worth
acting on" are three other verdicts, and each of those goes to an issue instead.

**Query this file, do not read it.** Once you have a candidate, grep the path or symbol you are
about to report on:

```
grep -in 'season_pruning\|plan_series_prune' .claude/skills/reaper-review/references/refuted.md
```

A hit means read the row and either accept it or beat it. No hit means report.

**Every row is pinned to a commit.** If the cited code moved since, the refutation is stale and
the candidate is live again: re-derive rather than trusting either answer.

**A row whose symbol changed FILE is re-pathed, and its commit pin stands.** The pin asks whether
the code changed, not whether it is still spelled at the same address, so a byte-identical
relocation leaves the refutation good and only the fingerprint wrong. Eleven rows moved with
`inspect`, `convert_list_protections` and `own_list_media_scope` when `engine/policy.py` split
into `policy_migrations.py` and `policy_warnings.py`; a fingerprint left at the old path returns
no hit, which reads to the next pass as "never refuted" and costs a full verify cycle each.

**A row may say OVERTURNED**, and that is not a bookkeeping detail — it is the most useful row in
the file. It means a later pass demonstrated the trigger after all, so the row now records *why
the refutation looked sound*, which is the mistake a fresh reviewer is about to repeat. Overturned
rows are kept rather than deleted for exactly that reason. Read one as a warning, never as a
refutation, and never cite it to close a candidate.

The two sections after the index are **not** refutations. Read their headings before using them.

## The index

| Fingerprint | Why the trigger cannot occur | Commit |
| --- | --- | --- |
| `frontend/src/App.tsx:Authed` — setup-gate-swallows-the-reap-stop | A reap in flight does NOT lose its Stop to the password step, because the state that swallows the controls is the state that prevents starting the reap. Zero local admins makes `complete` false on the FIRST page load, so `firstOpenStep` returns `password` and the operator never reaches the Reap button; the only exit sets a password, which flips `has_password` permanently (no route deletes a user, deactivates one, or clears a hash, and `admins.deactivate` refuses the last). Any install that CAN reap has `scan_ready` and `has_scanned` true, so once the password is set `complete` holds and the wizard never re-gates. Two residuals are real and are NOT this finding: a transient `/api/setup/status` failure taken as the way out, and deleting the last *arr mid-reap, which lands on the Connect step where Back and Skip both exist. The `ReapBar` docstring claiming it cannot be unmounted was true and was fixed separately. | `0ee2b64` |
| `frontend/src/components/RestoreCard.tsx:RestoreFlow` — restore-when-copy-is-stale | The pre-confirm "Restoring takes effect the next time the container restarts" is accurate, not stale, and rewriting it to promise a browser-driven restore would over-promise. `Restart now` sends SIGTERM to its own process (`api/backup.py`); preflight applies the swap on the NEXT boot, and whether a next boot happens is the container's restart policy, which Reaper cannot read from inside itself and says so. The armed-state sibling `RESTORE_ARMED_REST` was reworded deliberately because a button sits under it; no button sits under this one, so the sweep was already correct. | `0ee2b64` |
| `frontend/src/components/ReapPlan.tsx:ReapPlan` — blocker-notice-should-be-standing | `role="alert"` on the reap blockers is correct and `standing` would be wrong. The notice is inside `{run && …}`, so it appears only after the operator presses "Build a plan", and `App.tsx` gates the whole Dashboard on the `["setup"]` query, so it cannot arrive late: measured, it inserts in the SAME DOM batch as `.plan-summary`, in direct response to that press. The wizard's `standing` twin is justified by a local fact that does not hold here (its panel flips on a background poll and an `announce()` already carries the sentence). `ReapPlan` has no `announce()` at all, and its five sibling notices in `.plan-summary` are all non-standing, so `standing` would make it the outlier. | `0ee2b64` |
| `Dockerfile:CMD` — launch-inventory-complete | The launch inventory was walked, not assumed: `docker-compose.yml` sets no `command:`, `docker-entrypoint.sh` is `exec "$@"`, there is no Makefile, justfile, Procfile, fly.toml, systemd unit or helm chart, `.github/workflows/*` never boot the app, and no programmatic `uvicorn.run(` / `Server(` / `Config(` exists in `src/` or `scripts/`. Five launch sites, counted (rule 145). | `48f1eaf` |
| `docs/STATUS.md` — api-reference-line-is-stale | STATUS.md has never carried a line about the API reference, the API-key fence, or credentials, so a change to any of them leaves nothing stale there. Refuted independently at `2c3752a`, `9a3cb13` and `96b8114`. | `96b8114` |
| `frontend/public/vendor/scalar.js` — generated-asset-without-generator | Not a rule 68 gap. The bundle is gitignored (`.gitignore:231`) and rebuilt every run by the committed `frontend/scripts/copy-scalar.mjs` from `predev`/`prebuild`, so nothing committed can drift from it. | `2c3752a` |
| `frontend/src/api.ts` — hand-mirrored-backend-union | `api.ts` hand-mirrors backend `Literal` sets (`Verdict`, `ShowStatus`, `Override`, `SignalState`, `InstanceKind`, and others) and widens some to `string`, with no drift guard anywhere. That is the tree's standing convention, not a per-commit regression. Raise it only as a whole-tree proposal, never as a finding against one diff. | `f8592b3` |
| `frontend/src/App.tsx:Authed` — setup-gate-undefined-without-error | The `setup === undefined && !isError && !isLoading` fall-through (a paused or offline query) reaches the Dashboard with no setup status, but the old expression `isError \|\| (setup !== undefined && !setup.complete)` does exactly the same thing in that state. Identical before and after, so it is not a regression of the #181 divide. Raise it against `dev` as a whole-tree candidate or not at all. | `3f7613a` |
| `frontend/src/AppStaleRead.test.tsx:invalidateQueries` — act-wrapping-causes-a-flake | The intermittent failure of *keeps the Dashboard…* is not the change and not an `act(...)` gap. The failing output rendered the pre-PR sentence "…may be out of date. Reload to try again.", a string no longer in the tree, so `frontend/node_modules/.vite` was serving a stale module; `rm -rf frontend/node_modules/.vite` gives 604/604 green across repeated and parallel runs, and CI is green on `0ef3041`. Concurrent reviewer agents mutating `App.tsx` in the same worktree reproduce it too. The bare `await invalidateQueries` here versus `act(...)` in the `PlexPanel` twin is a spelling inconsistency, not a defect. | `3f7613a` |
| `frontend/src/components/PlexPanel.tsx:toggleLibrary` — stale-library-set-written-absolute | Issue #194, closed `Reviewed/Invalid`. The frontend half is real (keeping the grid through a failed refetch keeps its switches pressable, and `toggleLibrary` rebuilds the whole enabled set from `libraries.data`), but the write cannot reach the wrong server. `set_plex_libraries` (`src/reaper/api/plex.py:735`) never stores the payload: it walks the SERVER's own stored list and sets `lib["enabled"] = key in enabled`, so unknown keys are ignored rather than invented. And a switch to a different server has already called `set_plex_libraries(session, [])` (`src/reaper/services/plex_link.py:300`), so the stored list is empty and the press returns `[]`, which trips the auto-sync effect and loads the new server's libraries. Same end state as a normal switch. What is left is a switch that silently does nothing, which is #190's topic. **Filed at Priority/High from the frontend alone; the route was never opened.** | `d9ac0da` |
| `frontend/src/components/PlexPanel.tsx:libraries` — divided-test-spelled-two-ways | `libraries.isError && !libraries.data` and the sibling `!leavingSoon.data` are not a drift. Both branches are reached only once the query status is `success` or `error`, and `success` implies defined data, so the extra conjunct is redundant rather than wrong. `[]` is truthy, so an empty library list correctly renders the grid and its Refresh button instead of the never-loaded paragraph. | `3f7613a` |
| `frontend/src/components/ModalShell.tsx:ModalShell` — held-gate-suppresses-the-restore | `useDialogFocus`'s new `held` precondition cannot strand a modal's invoker. `held` is false only when focus is a real element OUTSIDE the panel, and a modal cannot be in that state: `.modal-scrim` is `z-index: 60` above everything, Tab is trapped, and `createPortal` appears exactly once in the frontend (`OverrideControls.tsx`), which no modal renders. Driven through `ReapConfirm`'s whole shape change — danger button disabled on click, progress replacing the form, then the report — focus falls to `<body>`, which `held` accepts. Rule 80 also holds: with a modal over an open `WhyShell`, `canClose={false}` closes neither layer. | `55ccce9` |
| `frontend/src/components/Notice.tsx:Notice` — alert-role-hides-its-children | `role="alert"` is a live-region subclass of `section`, not one of ARIA's presentational-children roles, so descendants (checkboxes, buttons, links) stay exposed and focusable. `role` is also emitted before the `{...rest}` spread and is absent from the props type, so a caller cannot override it. | `b752eff` |
| `frontend/src/components/Notice.tsx:Notice` — polled-alert-announces-repeatedly | `aria-live` announces on DOM mutation, and React does not rewrite an unchanged text node, so a poll returning the same string re-announces nothing. A label or `disabled` flip that follows an operator press is a discrete state change, which is what an alert is for. | `b752eff` |
| `frontend/src/components/PolicyRuleEditors.tsx` — surviving-listbox-role-is-dishonest | The last `role="listbox"`/`option` in the tree is honest, so the #172 sweep that stripped the other three correctly left it. This combobox implements the contract it announces: ArrowDown/ArrowUp move a roving `highlight`, Enter commits, Escape closes, and `aria-selected` tracks the highlight. Do not report it as a rule 72 twin left behind. | `55ccce9` |
| `frontend/src/components/ReviewQueue.tsx:DormantPill` — dormancy-copy-asserts-a-play | "Not watched in X" states an ABSENCE and is true whether or not a play exists, unlike the retired "watched too recently", which asserted an event. `reference_instant` returns `last_played or max(added_at, horizon)`, so the span can never reach past the evidence and every divergence understates dormancy, the keep direction. Same reasoning covers `signals.py`'s producer and `review._dormant_for`. A count printed as a lower bound is on the safe side of the same line: it asserts that at least N plays DID happen. | `3b499f5` |
| `frontend/src/components/ReviewQueue.tsx:groupKeyOf` — expanded-card-widens-a-bulk-action | A show card's expanded state cannot change what a bulk Spare or Reap covers: selection keys on the group (`isSelected={selected.has(groupKeyOf(group))}`), and no count or destructive set reads the card's `open` state. | `4e069b1` |
| `frontend/src/components/ReviewQueue.tsx:FilterChip` — chip-picker-unmounts-its-trigger | The twin of the ＋ Filter focus-return bug does not exist on the chip value-picker. Each dimension's `d.options` is built with its `defaultValue` filtered out, so no pick can return the dimension to its default and drop it out of `activeDimensions`: the chip that opened the picker always survives the press, and `closeMenu`'s `.focus()` lands. Only the ＋ Filter button can be removed by its own menu. | `55ccce9` |
| `frontend/src/components/GeneralPanel.tsx` — general-settings-copies-disagree | `["general-settings"]` is one cache entry with several subscribers (`GeneralPanel`, `JobsPanel`, `App`, `ReviewQueue`, `queueSettings`). React Query dedupes on the key, and the save path writes the canonical server response back with `setQueryData`, notifying every observer. There is no second key to drift. | `677c81e` |
| `frontend/src/components/Settings.tsx` — modal-draft-lost-to-a-section-switch | A draft inside `ModalShell` is unreachable while it exists: the scrim is `position: fixed; inset: 0; z-index: 60`, above every nav, with a `canClose` back guard (rule 80). Only panels that render inline need a dirty report. | `677c81e` |
| `frontend/src/components/WhyPanel.tsx:LeftForYou` — unparsed-detail-blanks-the-panel | A protection detail that does not match `/^could not check (.+?): (.+)$/` does not blank the block: it falls to the documented `kind: "raw"` row and renders verbatim. A parsed half that misses `CHECK_COPY`/`CAUSE_COPY` takes the documented raw fallback too. This is the only remaining wording read in `frontend/src/` and it is a display parse, not a discriminator. | `3b499f5` |
| `frontend/src/components/WhyPanel.tsx:keepRuleConflict` — arbitrary-pick-among-blocked-rows | At most one `season_progression` row can sit in `protections_unknown`, always at index 0: the gate is absent from `scan_runner.GATE_TYPES` so `build_gates` can never produce it, and the sole producer is `extra_results=(judgment.guard_result,)`, a one-tuple merged AHEAD of `evaluate_all` in `snapshot.py`. `.find` and `review._chip`'s first-match loop therefore pick the same entry. | `2dba0e4` |
| `frontend/src/components/WhyPanel.tsx:verdictLook` — held-season-renders-as-needs-a-look | A blanket season hold lands in BOTH `protections_fired` and `could_not_be_checked` (PROTECT plus `blocked=True`), and `verdictLook`'s Sanctuary branch returns before the conflict branch. jsdom-verified: renders "Sanctuary", never "Needs a look". | `2dba0e4` |
| `frontend/src/components/WhyShell.tsx:WhyShell` — aria-modal-hides-the-portaled-spare-menu | The phone panel's `aria-modal` (`:113`) cannot hide the body-portaled `SpareMenu` (`OverrideControls.tsx:397`), because the scoping follows FOCUS, not DOM ancestry. WAI-ARIA 1.2 makes the limiting a `MAY` and adds "if focus moves to an element outside the modal element, assistive technologies SHOULD NOT limit navigation to the modal element" — the opposite of the reported premise. Both engines implement exactly that, keyed on the focused element: WebKit's `updateCurrentModalNode()` walks up from `document->focusedElement()` and returns none (deliberate fix, WebKit bug 246580, 2022-10), Blink's `AncestorAriaModalDialog()` the same from `UpdateActiveAriaModalDialog`, and Gecko only exposes a `states::MODAL` flag and prunes nothing. Blink's pruning is Mac/Android only, so Chrome+NVDA/JAWS prunes nothing regardless. `useDialogFocus` (`OverrideControls.tsx:337`) focuses the menu, whose ancestor chain is `div.dur-menu → body → html`, so modality is off page-wide while it is open. Stale for WebKit older than ~Safari 16.2, which kept modality with focus outside. What survives is not the reported symptom: the menu is not a descendant of the dialog, which the same spec section makes a `MUST` for authors, and the menu's reading-order position is a separate open question (#206). | `f5497fc` |
| `frontend/src/components/WhyShell.tsx:WhyShell` — tab-trap-cuts-off-a-control | Trapping Tab in the phone `.why` sheet strands nothing. At ≤900px the panel is `fixed; inset: 0; z-index: 50`, and everything it could hide is beneath it: `.reap-bar` is `position: relative` in normal flow, `.bulk-bar` is sticky at `z-index: 20`, the phone nav is `z-index: 30`. `.scan-toast` is `z-index: 60` but `pointer-events: none` with no controls. Nothing interactive is visible-but-unreachable, and the trap is strictly better than the pre-fix state where Tab walked into invisible controls under the sheet. | `55ccce9` |
| `frontend/src/components/WhyShell.tsx:WhyShell` — two-why-panels-open-at-once | Two shells cannot mount together, so there is no double `aria-modal` and no double Escape. On Fairness, `openScalesPerson` clears `scalesUnmatched` and `openScalesUnmatched` clears `scalesUser`; in the review view the item and group panels derive from one `selected` state whose `kind` is either `"item"` or `"group"`. | `55ccce9` |
| `frontend/src/components/WhyShell.tsx:WhyShell` — dangling-aria-labelledby-branch | All six panels render the element carrying `headingId` in EVERY branch they can produce, so the dialog is never left unnamed. `WhyPanel`, `ShowPanel` and `NotInScanPanel` put the header above every fork; `ScalesPanel`'s `ProfileName` carries the id on both its link and plain-text returns; both fallbacks carry it in the error arm (`<h2>`) and the loading arm (the lead `<p>`, which has no heading to point at). No early return sits above any `WhyShell`, and `useId()` is called unconditionally at top level in all six. | `55ccce9` |
| `frontend/src/components/WhyShell.tsx:trapTab` — portal-double-trap | The portaled `SpareMenu` bubbling keydown through the React tree into the shell's `onKeyDown` is harmless. The menu's own `trapTab` runs first and moves focus synchronously, so by the time the shell's runs, `document.activeElement` is inside the portal — neither the aside's first nor last focusable nor the aside itself — and every arm is a no-op. | `55ccce9` |
| `frontend/src/components/WhyShell.tsx:WhyShell` — useid-colons-break-template-ids | React 19.2 emits `useId` values of the form `_r_0_`, with no `:` or `«»`, so `${menuIdBase}-${d.id}` composes a valid selector and the `aria-controls` assertions hold. No id-based `querySelector`/`getElementById` reads these anyway — `trapTab` walks the panel element. | `55ccce9` |
| `frontend/src/focus.ts:useDialogFocus` — suspense-swap-loses-focus | A Suspense fallback→real-panel swap, where both render a `WhyShell`, ends with focus on the real panel. React runs the deleted fiber's passive cleanup before the new fiber's passive mount within one commit, so the fallback's restore fires (focus back to the card) and the real panel's focus-in then runs, recapturing the card as its invoker. Verified in jsdom. | `55ccce9` |
| `frontend/src/focus.ts:useDialogFocus` — dialog-container-cannot-hold-the-ios-cursor | The reported symptom, an iOS VoiceOver cursor left on a control BEHIND a modal or the phone-width `.why` panel, cannot occur: that control is not in the accessibility tree while the surface is open. `useDialogFocus` focuses `panelRef`, and at both call sites `panelRef` IS the element carrying `aria-modal="true"` (`ModalShell.tsx:133-139`, `WhyShell.tsx:109-115`), not a wrapper around it and not a portaled sibling. WebKit's `AXObjectCache::updateCurrentModalNode` only ever returns a modal that contains the keyboard focus, so the walk up from the focused element finds that dialog immediately and modality is ACTIVE; every object outside it is then ignored through `ignoredFromModalPresence`, called from the cross-platform `AccessibilityObject::defaultObjectInclusion`, with no `PLATFORM(COCOA)` or `PLATFORM(IOS_FAMILY)` guard and no runtime flag (WebKit bug 246580, commit `b6eab27`, whose layout tests assert elements outside an active modal are inaccessible and reachable again once focus leaves it). This is the exact INVERSE of the `aria-modal-hides-the-portaled-spare-menu` row: there the focus target sits outside the dialog, `updateCurrentModalNode` returns none, and the page behind stays reachable, which is why #206 is real and this is not. Same root cause, opposite outcome, decided by where the focus target sits relative to the modal. What survives is not the reported symptom: iOS still cannot put the cursor on the container itself (`determineIsAccessibilityElement` needs `accessibilityElementCount == 0`), so the intent at `ModalShell.tsx:84-88` that reading starts at the dialog's NAME is unhonored on iOS and starts at the first control instead, which costs only which element inside the dialog is first. Do NOT apply #206's focus-the-first-descendant fix here on the strength of this candidate. Not device-verified and does not need to be for the symptom above; the iOS Simulator cannot answer it either, since `VoiceOverTouch.app` ships in the runtime and registers a launchd job but never acquires a pid. | `4297ea7` |
| `frontend/src/index.css` — unscoped-fair-selectors-leak | The phone media block's unscoped `.fair-*` selectors do compute on `ScalesPanel`, but they are grid-placement declarations and neither parent is a grid (`.scales-head-id` is flex, `.block` is block), so measured geometry at 320/390/500/640px is identical with and without the block. A latent hazard if either parent ever becomes a grid, not a present defect. | `60e035a` |
| `frontend/src/announce.tsx:announce` — double-announce-swallows-the-first | **OVERTURNED 2026-07-29 — the refutation was at the wrong layer, and the claim is now `Reviewed/Confirmed` as #193.** What is still true is the DOM finding: the real `useMutation` pair produces TWO React commits, so region 1 does hold "Movie policy saved." in a committed state. What that misses is that **screen readers never read the DOM**. They get events derived from a batched, diffed accessibility tree, and Blink batches for up to 150 ms (`kDelayForDeferredUpdatesAfterPageLoad`), emitting no live-region event itself — the browser process generates them by diffing successive trees, so `"" → text → ""` inside one batch is a net-zero diff and nothing fires. Measured in a real browser: both writes complete within 74 ms of the click in 10 runs out of 10, first-sentence dwell median 13.7 ms. Chrome + NVDA/JAWS loses it reliably. **The lesson for the next pass: a committed DOM state is not an announceable state, and `MutationObserver` cannot see the layer that decides.** Fixed by queueing in `announce.tsx`; the numbers are in `docs/LEARNINGS.md`. | `84f549a`, overturned at `a55b737` |
| `frontend/src/components/Notice.tsx:Notice` — warn-lead-mislabels-non-warnings | The `"Warning: "` sr-only lead does not mislabel any site. It adds no claim: `notice-warn` was already asserting caution in amber at all 37 sites, and the lead is the WCAG 1.4.1 fix that makes the existing claim available without color — so if the lead is wrong somewhere, the `tone` argument is wrong there, and gating the lead per-site would reopen the gap of two notices that look different and sound identical. Full classified sweep of the 37 shipped `tone="warn"` sites: 33 genuine cautions, 4 borderline, 0 mis-announced. Both cited sites survive on their own text — `WhyPanel`'s `KeptNotice` renders only when the Plex match is ambiguous or missing, a caution about unreadable evidence, and `RestoreCard`'s armed notice is a staged irreversible operation. What WAS stale is the `tone` docstring calling itself cosmetic, fixed separately. | `84f549a` |
| `frontend/src/main.tsx:queryClient` — failed-refetch-drops-the-panel | React Query keeps the last good `data` through a failed refetch, and nothing in the tree resets the cache: no `removeQueries`, `resetQueries` or `queryClient.clear()` anywhere in `frontend/src/`. So a `!data` branch means no successful load ever landed, and a poll failure never unmounts a form or crosses a mount boundary. | `677c81e` |
| `src/reaper/api/middleware.py:_api_key_allowed` — path-encoding-bypasses-the-fence | Under real uvicorn `scope["path"]` is percent-decoded before the guard sees it and Starlette's router matches that same string, so guard and router cannot disagree. 40 encoding probes over raw sockets (`%2F` splices, `..%2f`, `/./`, doubled slashes, `%00`, `;`, `#`, query smuggling, case) found zero bypasses. There is no `:path` converter, `Mount` or `root_path` in `src/`. The fence also does not soften when deletion is armed: 89 routes swept armed and unarmed, zero outcome changes. | `96b8114` |
| `src/reaper/api/middleware.py:_API_KEY_READ_DENY` — key-reachable-read-leaks-a-secret | Driven with a seeded webhook token and an *arr instance key: no stored secret appears in any key-reachable read. `InstanceOut` carries `has_key: bool`, `NotificationsOut` `has_webhook: bool`, `PlexStatusOut` no token, and every `SecretBox.decrypt` site uses the value for an outbound call without returning it. What the read scope DOES expose (every settings page, one person's viewing breakdown) is #117, not this. | `9a3cb13` |
| `src/reaper/api/middleware.py:_API_KEY_WRITE_ALLOW` — method-blind-write-allowlist | Refuted only for the table as it stood: the allowlisted paths carried exactly the unsafe operations their phrases name, no DELETE and no PATCH among them. **The allowlist keys on path alone**, so a NEW METHOD on an already-allowlisted path inherits the key with no edit here, and the generated-sentence tests catch a new path, not a new method. Re-derive if a method was added. | `96b8114` |
| `src/reaper/api/review.py:_candidate_out` — cache-skew-between-chip-and-decision | The chip and the reap decision come from ONE `_decode_explanation` result inside one `_candidate_out`, in one response, so no second key can drift between them. **This argument does not extend to the chip versus the WhyPanel**, and a later pass must not reuse it there: those are two keys and two requests (`["candidates", …]` from `GET /api/candidates` against `["candidate", id]` from `GET /api/candidates/{id}`), `candidate_detail` loads the row with no snapshot filter, and the two were driven disagreeing across two snapshots of one title. Rule 79's named shape, pre-existing. | `3b499f5` |
| `src/reaper/api/review.py:_chip` — could-not-check-prefix-skips-a-conflict | `_chip`'s outer `not detail.startswith("could not check")` guard measured `False` for all three `PruneConflict.message` shapes, so every one reaches the `season_progression` branch. Every other blocked producer in `src/` does start with that prefix, which is what the guard is there to skip. | `3b499f5` |
| `src/reaper/api/review.py:_explanation_out` — extra-explanation-key-breaks-the-model | Adding a key to a stored explanation entry does not break the wire: no `GateOutcomeOut(**entry)` splat exists, `_explanation_out` does `Explanation(**decoded)` and pydantic validates field by field, and the only `extra="forbid"` in `src/` is on policy bodies, which never see an explanation. An unknown key is silently dropped by `model_dump()`. `Explanation` is constructed at exactly one site and served by one route. | `2dba0e4` |
| `src/reaper/api/simulate.py:_has_blocked_protections` — reworded-detail-breaks-a-classifier | `_has_blocked_protections` tests presence and shape only, and `_primary_reason` prints `_detail_of(unknown[0])` verbatim. Neither classifies on wording nor reads a key inside an entry, so a reworded detail or an added field is invisible to both. | `3b499f5` |
| `src/reaper/api/schemas.py` — three-state-collapses-on-the-wire | A `bool \| None` field survives serialization as three distinct states: literal response bytes measured `true`, `false` and `null`. Repo-wide there are zero uses of `exclude_none` / `exclude_unset` / `exclude_defaults` / `response_model_exclude` in `src/` or `frontend/src/`, and `api/schemas.py` declares no `model_config` / `ConfigDict`. | `2dba0e4` |
| `src/reaper/api/schemas.py:GateOutcomeOut` — gate-id-narrowed-on-the-seam | The gate id and detail are plain `str` on the route, `string` in `api.ts`, and round-trip unnarrowed. `GATE_META` is read only for `protected_by`, and every `GateId` member now has an entry (`policyMeta.ts` closes the map with `satisfies Record<GateId \| "hand_spare", GateMeta>`), so an unmapped id cannot break a lookup. The conclusion stands. **One clause of the original reasoning is false and must not be reused: "a blocked gate never appears there."** `season_evidence.guard_result` returns a *blocked* PROTECT for a season whose progress guard could not be answered, and `Evaluation.protectors` selects on `outcome == PROTECT` alone, so it does reach `protections_fired` and `simulate._fired_gates` tallies it — which is how #551 came to label those rows "Your season rules" when no season rule kept them. Corrected at `35cb28e`; the row's own pin is unchanged because the narrowing question was never touched. | `3b499f5` |
| `src/reaper/api/settings.py:put_general` — omitted-field-written-as-default | `GeneralSettingsIn` declares every field `\| None = None` and each write is guarded by `is not None`, so a field the client omits is left alone. All validations run before all writes, which share one `session.commit()`, so a 422 on the last field leaves the whole `GET` payload byte-identical. `_refresh_proxy_state` re-reads the STORED switch rather than the payload, so a list-only patch still arms the live middleware. | `ef0278d` |
| `src/reaper/api/tags.py` — seerr-is-internal-vocabulary | Not a rule 21 breach: "Seerr" is the app's own operator-facing word. `ServiceModal.tsx` labels the service "Seerr" and `ReviewQueue.tsx`'s tooltip says "through Seerr". | `2c3752a` |
| `src/reaper/auth/cookie.py:_forwarded_proto` — appended-forwarded-header-outranks-the-proxy | `Headers.get` returns the first `X-Forwarded-Proto`, but nginx, Traefik, HAProxy, Envoy, ALB and Cloudflare all set or overwrite rather than append. No `CORSMiddleware` is registered anywhere in `src/`, so a cross-origin page cannot attach the header past preflight either. | `48f1eaf` |
| `src/reaper/auth/cookie.py:is_secure_request` — https-branch-changes-behavior | Every behavior change in this function is confined to `scheme=https`, which **no shipped launch can produce**: there is no `--ssl-keyfile` anywhere in the tree, and `--no-proxy-headers` removes the only middleware that rewrites the scheme. All eight `scheme=http` cells are identical old versus new. **Re-derive this first if a TLS-terminating launch is ever added** — it is the single fact three candidates rest on. | `48f1eaf` |
| `src/reaper/auth/cookie.py:read_session_tokens` — cookie-name-flip-strands-a-session | The cookie NAME follows `is_secure_request`, but that is consulted only at the three sign-in sites, never on a per-request refresh, so the name can change only when a fresh token is issued, and that write always deletes the other name. `read_session_tokens` returns both names on every request regardless of scheme, and `clear_session_cookie` ignores the request. | `48f1eaf` |
| `src/reaper/clock.py:humanize_window` — window-renders-oddly-at-an-edge | Checked at every edge: 1 gives "day", 30 "month", 365 "year", 366 "1 year, 1 day", 400 "1 year, 1 month". `ge=1` makes 0 and non-integers unconstructable, and a negative or sub-1 value gives "less than a day". `TIME_UNITS` uses month=30 and year=365, exactly `humanize_days`' arithmetic, so the two can never disagree by a unit and lowering a window to the number the copy names always clears the block. | `f8592b3` |
| `src/reaper/db/models.py:Snapshot.horizon_at` — null-horizon-crashes-the-scan-or-simulate | `horizon_at` is `nullable=False` in the frozen Alembic baseline and `_latest_snapshot` does a full entity load, so neither the scan nor the simulate route can see a `None` there, and there is no deferred-column IO. The three `history_reach_days(...)` call sites all take a non-optional `datetime`, and `snapshot.py` resolves `mirror.earliest is None` to `utcnow()` before building the `ScanContext`. | `394cc3a` |
| `src/reaper/engine/dormancy.py:history_reach_days` — negative-or-absurd-reach | A zero, negative or clock-skewed reach fails closed everywhere it is read: it can never clear `reach.value >= needed`, so the gate blocks; `humanize_days(-1)` renders "less than a day"; `history_sync` drops a row whose date and started are both falsy, so epoch 0 cannot enter the mirror; and in `backtest.facts_as_of` a cutoff preceding the horizon makes `dormancy_days` negative and returns `None` before any `Facts` is built. | `3b499f5` |
| `src/reaper/engine/dormancy.py:history_reach_days` — dual-clock-drift | The reach is frozen on `ScanContext` while `days_since_added` samples `utcnow()` per item, and the policy editor recomputes it from the same function against the same cache engine. Both are `int`-floored off one DB; asserted equal across horizons 0/1/89/90/364/365/1094/1095 and four sub-day offsets. At most one day of drift, and it raises `needed`, which blocks more — rule 104's dual clock in the safe direction. | `f8592b3` |
| `src/reaper/engine/dormancy.py:reference_instant` — dormancy-outruns-the-mirror | Dormancy is clamped to `max(added_at, horizon)`, so it can never exceed the mirror's reach and every divergence from the deeper truth UNDERSTATES it. That is the keep direction on a `gte` condemn rule and an `lte` protect rule alike, which is why `days_unwatched` needs no `reach_span`. | `65359be` |
| `src/reaper/engine/fields.py:CustomProtectGate` — window-days-none-reaches-the-shortfall | `CustomProtectGate` is constructed at exactly one production site (`scan_runner.py`, with the policy's window), simulate included through the same `build_gates`. `evaluate_custom` / `evaluate_keep` are reached only from `score()`, whose parameter is typed `int`. The `None` arm is a fail-closed default for an unforeseen caller. | `65359be` |
| `src/reaper/engine/fields.py:evaluate_rules` — lane-composition-is-wrong | Moot: `evaluate_rules` has no caller in `src/` at all (test-only). Production custom condemn rules go one condition at a time through `evaluate_custom` then `fields.evaluate`. | `65359be` |
| `src/reaper/engine/fields.py:_survives_more_history` — op-direction-backwards | All four outcomes walked against both lanes: a lower bound already clearing a `gte` floor stays clear, one already exceeding an `lte` ceiling stays exceeded, and the two overturnable outcomes are exactly the dangerous pair. `NUMERIC_OPS` is exactly `(GTE, LTE)`, both handled, and the `case _:` returns `False`, so a future numeric op blocks rather than passing. | `65359be` |
| `src/reaper/engine/gates.py:Facts` — a-fact-builder-was-missed | There are exactly four `Facts(` construction sites in `src/` (`snapshot`, `season_scan`, `backtest`, and the `facts_codec` thaw) and `calibration.py` builds none. `facts_codec` picks a new field up automatically from its `Observation[...]` annotation, and a row written before the field thaws `Unknown`, which reads as un-checkable rather than as a wrong definite value. `fields.REGISTRY` is hand-written, so a new fact is not operator-authorable until it is added there (rule 35). | `65359be` |
| `src/reaper/engine/gates.py:Facts.distinct_watchers` — absent-observation-hole | `distinct_watchers` cannot be `Absent` in production: `snapshot.py`, `season_scan.py` and `backtest.py` all write `Known` or `Unknown`, never `Absent`, so `fields.evaluate`'s neither-matched-nor-blocked hole is unreachable from a shipped builder. | `94f11fc` |
| `src/reaper/engine/gates.py:Facts.distinct_watchers_all_time` — unswept-all-time-reader | Every site swept: the field is display-only, no built-in gate reads it, `signals.py` records that it cannot reach the signal lane, the operator-authored field goes through `reach_shortfall` then `lifetime_shortfall`, and `backtest` builds it from a complete play list. The season roll-up was the one missing reader and is wired. | `3b499f5` |
| `src/reaper/engine/gates.py:GateResult` — trailing-defaulted-field-breaks-construction | Adding a trailing defaulted field to a `frozen`/`slots` dataclass (`GateResult`, `PruneConflict`) breaks nothing: no site in `src/` or `tests/` constructs either with four or more positional arguments, there is no `replace` / `astuple` / `asdict` / `fields()` over them outside the codec, and neither is ever a set member or dict key, so a changed `__eq__` / `__hash__` is inert. | `3b499f5` |
| `src/reaper/engine/gates.py:GateResult.defers_to_owner` — flag-reaches-a-delete-decision | Display-only. The field appears in zero lines of `verdict.py`, `executor.py`, `planner.py` and `condemned.py`; a repo sweep finds two writers (`season_scan`, `snapshot`), the codec pair, `review._chip`, `GateOutcomeOut` and `WhyPanel.conflictNote`. A change to how it is coerced cannot alter a condemn/abstain/protect or a hand reap. | `fbb360e` |
| `src/reaper/engine/gates.py:lifetime_shortfall` — non-known-age-takes-the-permissive-branch | The guard is `not isinstance(age, Known)`, which `Absent` fails as well as `Unknown`, so neither reaches the permissive branch. Drove all four inputs: a non-`Known` age returns "this scan did not record when it was added", a non-`Known` reach returns "this scan did not record how far back your watch history goes". | `3b499f5` |
| `src/reaper/engine/gates.py:MinDormancyGate` — dormancy-floor-masks-the-reap-path | Condemning under the shipped 1095-day floor requires dormancy at or above 1095, and dormancy is clamped to the reach, so the reach already spans any window of 1095 days or less. Below the floor `MinDormancyGate` PROTECTs every item, and PROTECT beats blocked. **The masking covers only readers whose span is a WINDOW.** It does not cover the all-time count, whose span is the item's AGE: an item added far earlier than a reach that still covers its dormancy has every shipped gate answering normally while a `watchers_all_time` protection returns blocked. | `65359be` |
| `src/reaper/engine/policy.py:GateSetting._protective_floors` — threshold-zero-protects-everything | Unconstructable: `_protective_floors` raises for `SERVER_POPULARITY` with `threshold < 1` while enabled, so the gate cannot be configured to PROTECT the whole library. | `f8592b3` |
| `src/reaper/engine/policy.py:PolicyBody.model_copy` — validator-skipped-on-copy | True of `model_copy`, which skips validators, and pinned by an existing test — but `PolicyBody.model_copy` has zero call sites in `src/`. No production path reaches it. | `f772a44` |
| `src/reaper/engine/policy.py:_EVIDENCE_REPLAYABLE_FIELDS` — simulator-serves-a-stale-preview | The replay list is an allow-list: a field it omits moves `evidence_hash`, so the replay tier is skipped and simulate falls to a lower tier that returns the reason rather than a number. Tier 3 refuses to simulate per-item at all when the edit touches a watch window or any gate, so frozen counts can never be re-phrased under a draft window. | `8ff0a3e` |
| `src/reaper/engine/signals.py:evaluate_keep` — keeps-corrupt-coverage-accounting | `score()` computes coverage over `results` only; keeps are deliberately excluded, and `KeepResult.evaluated` is read only by the stored-explanation writer. | `65359be` |
| `src/reaper/engine/verdict.py:decide_verdict` — blocked-gate-widens-deletion | A blocked `CustomProtectGate` is `ABSTAIN, blocked=True`, which `decide_verdict` resolves to abstain, and `GateId.CUSTOM` is absent from `DEFERRABLE_BLOCK_GATES`, so it holds a hand reap too. There is no path from a blocked gate to condemn. | `65359be` |
| `src/reaper/engine/verdict.py:decide_verdict` — executor-re-derives-a-verdict | Neither the executor nor the planner imports `evaluate_all` or `decide_verdict`, and no `Facts` reaches the executor. Blocked, then abstain, then not planned: an engine-side change cannot reach a send by that route. | `394cc3a` |
| `src/reaper/main.py:lifespan` — cache-engine-missing-on-some-path | `app.state.cache_engine` is set unconditionally in `main.lifespan` before any route serves, beside `session_factory`, which every policy route already dereferences. A client that skipped lifespan fails there first. | `f8592b3` |
| `src/reaper/main.py` — router-tags-change-what-a-route-does | A router-level `tags=` cannot change route behavior: `route.tags` is written by FastAPI's routing and read only by its OpenAPI generation, and `generate_unique_id` derives an `operationId` from `route.name`, `path_format` and method, never from tags. The middleware's fences key on method plus path, which tags do not touch. | `2c3752a` |
| `src/reaper/services/backup.py` — settings-shape-strands-backup-or-restore | A changed app-setting shape cannot strand either: there is no per-key settings export, `backup.py` copies the whole database file so the row rides along untouched, and `restore.py` names exactly one app-setting key (`DESTRUCTIVE_KEY`). Nothing enumerates or reshapes app settings. | `4e069b1` |
| `src/reaper/services/condemned.py:reap_override_verdict` — unreadable-explanation-held-reaps | A pydantic validation failure on an explanation never touched a decision: `override_effective` comes from `reap_override_verdict`, which parses with `json.loads`, as do `_has_blocked_protections` and `_fired_gates`. `explanation_unreadable` has one consumer, a notice, and no control gates on it. | `fbb360e` |
| `src/reaper/services/executor.py:_finalize_plex` — plex-rescan-scoped-too-wide | The movie rescan is scoped to the movie's own folder: it passes `movie["path"]`, which in Radarr is the movie folder, falling back to `folderName`. For a flat season layout `_common_parent` returns the series root, which is correct rather than a gap — when every episode sits directly in the series folder, that folder genuinely is the narrowest directory holding the deleted files. | `59188c9` |
| `src/reaper/services/executor.py` — none-size-in-the-growth-check | `live_sizes` can hold `None`, and both branches handle it before any delete: the whole season is skipped when any size is `None` and an approved size exists, and `None` is filtered out of the sum. | `59188c9` |
| `src/reaper/services/history_sync.py` — short-page-truncates-the-mirror | `rows = page.get("data") or []` then `if not rows: break` cannot make items look more dormant. Three independent controls: `_check_regression` runs the same call shape first and raises `HistoryRegressionError` (a `RuntimeError`, which `scan_runner`'s `IntegrationError` handler does not swallow); a mirror truncated to nothing degrades the scan via `mirror.earliest is None`; and the direction is inverted anyway, since a truncated walk raises the horizon toward now, which SHRINKS observed dormancy. `INSERT OR REPLACE` with no deletes means an existing mirror cannot lose depth. **The identical coercion in `clients/seerr.py` WAS dangerous** and was fixed in `0dea343`: the shape is safe here, not everywhere. | `be72828` |
| `src/reaper/services/library_index.py` — short-page-drops-index-items | An item missing from the index has no `rating_key`, so its facts are Unknown, it abstains, and the executor spares a keyless item. The plexapi sweep in the same function unions the missing rows back in through a complete-or-raise pager. No condemn path exists. | `be72828` |
| `src/reaper/services/planner.py:manifest_hash` — added-field-moves-a-hash | Nothing hashes an explanation, a gate result or a facts dict. `manifest_hash` is over `(media_key, size_bytes)`, and `policy_hash` / `scoring_hash` / `evidence_hash` are over `PolicyBody`. A full `hashlib` / `sha256` sweep of `src/reaper/` finds no other candidate-derived digest, so adding a stored key cannot break an interlock. | `2dba0e4` |
| `src/reaper/services/scan_runner.py:build_gates` — gate-switch-drops-the-season-hold | The season guard rides in `extra_results`, merged ahead of `evaluate_all` in `snapshot.py`, and never goes through `build_gates`, so an operator turning the `season_progression` gate off cannot remove it. `SEASON_PROGRESSION` is absent from `GATE_TYPES` for the same reason. | `8ff0a3e` |
| `src/reaper/services/season_pruning.py:plan_series_prune` — season-both-protected-and-conflicting | Each on-disk season goes into exactly one of `prunable` / `protected`, and conflicts key on `pruned_season` being in `prunable`, so a season can never be in both and an ordering choice can never be the tiebreaker. | `a191086` |
| `src/reaper/services/season_pruning.py:plan_series_prune` — progress-established-default-is-fail-open | `progress_established: bool = True` is reached by one caller that omits it, the offline first pass, and that plan is never read for a deletion decision: `_SeriesWork.plan` has no reader after construction beyond `fully_protected`, which only skips a step that fills `season_final_episode` — skipping it drops the guard to whole-season protection, which keeps more. That pass also passes no `watchers_by_season`, so `_detect_conflicts` returns `[]` regardless of any shortfall map. | `3b499f5` |
| `src/reaper/services/season_pruning.py:SeriesPrunePlan.auto_approvable` — blocked-results-distort-a-count | No consumer of a blocked gate result is count-shaped. `auto_approvable` has zero consumers in `src/`; `Evaluation.blocked`, `condemned.reap_override_verdict_decoded`, `simulate._has_blocked_protections` and `WhyPanel.LeftForYou` are boolean- or list-shaped; `plan.conflicts` has exactly one reader. Blocked, then abstain, then not condemned, then never planned, so nothing reaches the executor or the cap math. | `3b499f5` |
| `src/reaper/services/season_scan.py:active_progress` — progress-windowed-to-the-popularity-window | The `pairs` and `progress` queries feeding `progress_by_user` / `last_watched_by_user` carry no `since` predicate, so the mirror horizon is their only span bound and the mirror reach is the correct comparison. | `8ff0a3e` |
| `src/reaper/services/season_scan.py:build_season_facts` — season-block-widens-a-show | Seasons are their own `Candidate` rows with their own `Facts`; episodes are never judged individually, and `season_pruning` reads no gate results. An unresolved season's `distinct_watchers` is `Unknown`, so the blocked arm fires before any count check. | `394cc3a` |
| `src/reaper/services/season_evidence.py:guard_result` — hold-days-zero-is-incoherent | `in_progress_hold_days = 0` disables TV season pruning outright rather than making the guard incoherent: `active_progress` returns every visible viewer, the establishable predicate returns `False`, and the planner holds everything. Keep direction, and stated in both the policy docstring and the editor help. | `8ff0a3e` |
| `src/reaper/services/snapshot.py:ScanContext` — degraded-mirror-reads-optimistic | Every degraded-mirror arm fails closed. `mirror.earliest is None` sets the horizon to `utcnow()`, giving a reach of 0, AND degrades the whole scan ("no watch history at all: nothing can be judged"), which reaches the UI as `SnapshotOut.degraded` and blocks execution; a stale ingest degrades too; a future horizon gives a negative reach. `horizon` and `reach_days` come from one `ScanContext` construction and cannot disagree. | `8ff0a3e` |
| `src/reaper/services/snapshot.py:_explain` — blocked-result-missed | `Evaluation.could_not_be_checked` is exactly `[r for r in results if r.blocked]`, so `_explain` cannot miss one. | `a191086` |
| `frontend/src/components/DeletionToggle.tsx:toggle.onSuccess` — announces-from-vars-not-the-response | `RuntimeSafety.destructive_allowed` is a plain alias of `destructive_enabled` (`src/reaper/config.py:357-359`) and `PUT /api/settings/safety` writes then re-reads it (`src/reaper/api/settings.py:917-920`), so a 200 can only mean the requested direction was honored; `vars.enabled` is a faithful proxy. | `5f41ff7` |
| `frontend/src/components/DeletionToggle.tsx:rowButtonRef` — one-ref-two-buttons-goes-stale | "Turn on…" and "Turn off" are mutually exclusive arms of one ternary in one child slot and both are `<button>`, so React reuses the host node across the arm; focus survives the refetch that flips the label. | `5f41ff7` |
| `frontend/src/components/DeletionToggle.tsx:returnToRow` — strands-focus-on-body | The effect is declared above every early return and both exits (`closeForm`, arm success) flip `confirming`, so it always runs. Only a simultaneous safety-read failure could null the ref, and that state was the separate green-block finding fixed at this commit. | `5f41ff7` |
| `frontend/src/components/ReapConfirm.tsx:armStage` — focus-restolen-while-typing | The effect's dep is a boolean, not a value, so typing, ticking the trash box and the 1s status poll all leave it `true` and it does not re-run. Still true with `trashOk` added to the condition, which is likewise a boolean. | `5f41ff7` |
| `frontend/src/components/ReapConfirm.tsx:outcomeRef` — ref-attached-to-two-divs | `report` and `failed` cannot both be truthy on any shipped path: `status.report` is set only on the non-error branch (`src/reaper/api/runs.py:585-587`), and both `except` arms (`:615`, `:625`) leave it at the `None` assigned at `:492`. | `5f41ff7` |
| `frontend/src/components/ReapBar.tsx:ReapBar` — comment-claims-an-end-announcement-that-cannot-fire | Rule 7/24 holds for the case the comment reasons about: `exec.onSuccess` seeds `["reapStatus"]` with the server's `running: true` (`runs.py:481-484` sets it before returning) and the always-mounted bar observes the same key, so `ranRef` is set before any end can arrive. The one gap, a run this tab never saw running, is the case the comment explicitly excludes. | `5f41ff7` (the component moved out of `App.tsx` in #603's phase byte-identically, so the pin still holds) |
| `frontend/src/components/ReapBar.tsx:ReapBar` — announce-misses-a-terminal-phase | The phase vocabulary in `runs.py` is `idle | reaping | complete | aborted | error`; all three terminal ones are branched, and the `idle` reset at `runs.py:535` also nulls `run_id`, which the effect's first guard catches. | `5f41ff7` (the component moved out of `App.tsx` byte-identically since, so the pin still holds) |
| `frontend/src/announce.tsx:drain` — queue-drops-the-last-message | `drain()` shifts one and re-arms whenever `pending` is non-empty; the terminal call finds it empty and leaves `drainTimer` null, so the next `announce()` speaks in the same tick. Nothing is lost. | `5f41ff7` |
| `frontend/src/announce.tsx:drain` — reentrant-drain-orphans-a-timer | Would require a passive effect to call `announce()` synchronously inside `emit()`; `useSyncExternalStore` schedules its re-render rather than flushing it inline outside a React event, so the window does not open. Could not be constructed. | `5f41ff7` |
| `frontend/src/announce.tsx:announce` — queue-floods-or-speaks-stale-state | FIFO with a single 400ms drain. The reap sheet throttles to phase changes and tenths, `ReapBar` fires once per run behind `settledRef`, and `useJobFlash` fires on a transition its own mount saw. No caller can outpace the drain. | `5f41ff7` |
| `frontend/src/focus.ts:useDialogFocus` — in-panel-focus-moves-break-the-invoker-restore | `held` accepts any element the panel contains, and both `phraseRef` and `outcomeRef` are inside it, so the close restore still fires. | `5f41ff7` |
| `frontend/src/focus.ts:useRemovalFocus` — index-does-not-match-the-marker-set | All five call sites check out: one marker per row in every container (`PolicyEditor.tsx:113`, `:407`, `PolicyRuleEditors.tsx:393`, `:688`/`:715`, `ReviewQueue.tsx:190`/`:2302`); the two-array list offsets by `conditions.length + i`; the search chip's fixed index 0 and the dimension chips' `chipIndex + (search ? 1 : 0)` match DOM order; and every empty-list case falls back to a control that provably exists at that moment. | `5f41ff7` |
| `frontend/src/focus.ts:useSavebarFocus` — global-savebar-selector-collides | `document.querySelector(".savebar")` can match only one bar: `PolicyEditor`'s bar and `GeneralPanel`'s (each the one `<div className="savebar">` in its file) are on mutually exclusive views, and each page renders one bar for all its dirty halves. The same reasoning refutes the sibling candidate that the guard fires early while a bar is still up. | `5f41ff7` |
| `frontend/src/components/CardOpen.tsx:CardOpen` — name-does-not-contain-visible-text | All four sites satisfy WCAG 2.5.3: `Fairness.tsx:110`, `ReviewQueue.tsx:941`, `:1026` and `:1225` each render the title inside a name that contains it; the year and every chip are rendered outside the control. | `5f41ff7` |
| `frontend/src/components/CardOpen.tsx:CardOpen` — pressHandledByCard-set-wrong | Set exactly where the container's own `onPointerDown` acts (`MovieCard` 1013, `ShowCard` 1204, both ancestors of their `CardOpen`), and absent at the two sites with only an `onClick` (`Fairness.tsx:110`, `SeasonList` 941). `pressed` is `undefined` outside Select mode at all four. | `5f41ff7` |
| `frontend/src/components/CardOpen.tsx:CardOpen` — heading-text-now-includes-the-label | The `<h3 className="card-title">` name-from-content folds in the button's `aria-label`. Real but cosmetic, and the direct consequence of the WCAG 2.5.3 constraint the component was built around. | `5f41ff7` |
| `frontend/src/components/ReviewQueue.tsx` — orphan-tabindex-after-role-removal | Grepped every `tabIndex` and `role="button"` in `frontend/src`: no `tabIndex={0}` survives on any roleless container, and the two `onKeyDown` Enter/Space guards that existed only to defend against the removed container handlers were removed with it. | `5f41ff7` |
| `frontend/src/components/ReviewQueue.tsx:SeasonStrip` — removed-key-guard-strands-a-control | Nothing above either control handles Enter/Space any more: the card head and the `<li>` both lost theirs in the same diff, so the removals are correct and rule 60's own text was updated to say so. | `5f41ff7` |
| `frontend/src/components/ReviewQueue.tsx:MovieCard` — at-synthesized-click-dead-in-select-mode | A bare `element.click()` with no `pointerdown` cannot select a card while `pressHandledByCard` is true, but the pre-PR container had the same hole (`onClick={() => !selectMode && …}`), so the change neither introduces nor widens it. | `5f41ff7` |
| `frontend/src/useMediaQuery.ts:PANEL_OVERLAY_QUERY` — js-css-breakpoint-mismatch | `1100px` matches the `main.split .why { position: fixed }` block at `index.css:1365` and `900px` matches `index.css:2194`; the CSS diff changes no breakpoint. `WhyShell` is the sole reader of the 1100 and `App`/`ReviewQueue`/`Settings` the only readers of the 900. | `5f41ff7` |
| `frontend/src/components/WhyShell.tsx:WhyShell` — aria-modal-over-visible-cards-900-to-1100 | In the new band the panel is a 440px sheet with cards still visible and mouse-clickable while `aria-modal` and the Tab trap declare them inert. Deliberate and documented (`WhyShell.tsx:9-23`), and strictly better than tabbing into controls hidden under the sheet. | `5f41ff7` |
| `frontend/src/components/ScanBar.tsx:ScanRow` — progressbar-omits-valuetext | The `ScanLine` twin omits it too; only `ReapBar` and `ReapConfirm` carry one, and `PolicySimulator` says in a comment why it does not. No divergence to fix. | `5f41ff7` |
| `frontend/src/components/ScanBar.tsx:ScanRow` — valuenow-reports-zero-for-unknown | `pct ?? 0` can only render inside `scanning`, where `status` is non-null and `percent` is a number; the `?? 0` is defensive, not a wrong definite value. | `5f41ff7` |
| `frontend/src/components/GeneralPanel.tsx:GeneralPanel` — savebar-ref-below-an-early-return | The heading carrying `bar.ref` renders after two early returns, but `document.body.contains(null)` is false on those branches so `leaving()` is inert. Rule 146's shape is honored: the hook itself is above every return. | `5f41ff7` |
| `frontend/src/components/ServiceModal.tsx:TestBadge` — a-fourth-test-result-goes-unannounced | Grepped every `setTest(r)` site: three, and all three announce `testSentence(r)`. | `5f41ff7` |
| `frontend/src/index.css` — has-selector-lights-two-things-at-once | A focused show title matches both the `.card.clickable:has(…)` border rule and the `.card-head:has(…)` wash. `.card-body` is a direct child of `.card-head` as the selector assumes, and the double treatment is the stated intent, not a bug. | `5f41ff7` |
| `frontend/src/announce.test.tsx` — weakened-one-region-test | The test now announces once so it no longer discriminates, but the property it dropped is asserted by two of the new queue tests. Redundant, not a coverage hole. | `5f41ff7` |
| `frontend/src/components/ReapConfirm.tsx:canExecute` — gate-widened-by-the-a11y-sweep | Untouched by the diff: the execute gate and the content-bound phrase comparison are byte-identical to `origin/dev`. | `5f41ff7` |
| `frontend/src/index.css:.card-title` — unbroken-title-paints-out-of-its-card (issue #213) | Measured against a real snapshot rather than a synthetic string: across several thousand distinct `candidate.title` values in 4 libraries and 31 snapshots, the longest run with no space is 17 characters and p99 is 12, against a card threshold of roughly 45 at a 500px viewport. `group_title` maxes at 13. No real title comes within 2.6x of overflowing, so the asserted loss does not occur. Closed `Reviewed/Invalid`. The separate rule 139 gap — `.card-title` carrying no `overflow-wrap` while its rule-139 siblings do — is real and filed on its own. | `5f41ff7` |
| `frontend/src/components/PolicyEditor.tsx:ANCHORS` — in-progress-warning-renders-twice | `in_progress_hold_days` is claimed by exactly one anchor, and the anchor walk asserts exactly-once across every guard branch including the held and absent arms. | `2367a30` |
| `frontend/src/components/PolicyEditor.tsx:ANCHORS` — min-dormancy-warning-lands-in-the-catch-all | The new `gates.min_dormancy.threshold` warning is claimed by the existing `gates` anchor's `prefix: "gates."`, not dropped to the foot of the page. `GateId.MIN_DORMANCY.value` is literally `min_dormancy`; the anchor's `WarnBlock` declares no guard and sits under the `<ul>` of `GateRow`s; and `GATE_META.min_dormancy` carries `unit: "days"`, so `GateRow` renders the threshold box whenever the gate is enabled — the only state the backend emits in. It cannot double-render either: `unanchoredWarnings` filters against the guard-filtered anchors and `gates` is unguarded. | `2367a30` |
| `frontend/src/components/PolicyEditor.tsx:PolicyEditor` — in-progress-describedby-dangles | The new `describedBy` on the hold-days box cannot point at a missing id. It sits inside `{draft.keep_in_progress && …}` while the `WarnBlock` sits OUTSIDE that subtree, so the block's mounted set is a strict superset of the input's. Matches the `keep_last` / `max_unmeasured_per_run` sibling contract (rule 72); `keep_last` filters by field because its block serves two controls, `in_progress` claims one so unfiltered is right. | `2367a30` |
| `src/reaper/engine/fields.py:reach_shortfall` — assert-never-crashes-a-scan | The two new `assert_never` arms cannot be reached at runtime by a stored policy. Both read `FieldSpec.reach_span` off the hardcoded `BY_KEY` registry, never a serialized value; a policy can only name a field KEY, and an unknown key is filtered before the match (`BY_KEY.get(...) is None → continue`). mypy is exhaustive over the enum today, so the arms are a type-level guard rather than a live branch. | `2367a30` |
| `src/reaper/engine/gates.py:progress_is_establishable` — move-creates-an-import-cycle | Moving the predicate out of `services.season_pruning` into `engine.gates` introduces no cycle: `gates.py` imports nothing from `fields`, `signals`, `verdict`, `policy` or `services`. `mypy src/reaper` and the full suite are green. | `2367a30` |
| `src/reaper/engine/policy_warnings.py:inspect` — dormancy-and-window-warnings-stack | The new dormancy-floor warning and the popularity-window one are mutually exclusive by construction: `window_short` is computed only when `reach_clears_dormancy`, which is the exact negation of the dormancy branch's guard. The two deictic remedies ("lower this wait" / "lower the window") can never appear together. | `2367a30` |
| `src/reaper/engine/policy_warnings.py:inspect` — dormancy-warning-fires-while-items-are-condemnable | The claim "nothing will be flagged" is sound whenever the branch fires. **Conclusion still holds; two premises were wrong and are corrected here (at `fa94ad5`), so do not cite this row's mechanism.** `reference_instant` is no longer `last_played or max(added_at, horizon)` — since #272 it measures from `last_played`, else `max(added_at, horizon)`, else nothing at all; both *measurable* arms are at most the reach, and the third yields no number, so "the most dormant any item can read IS the reach" survives. And `MinDormancyGate` does **not** PROTECT on Unknown: `gates._blocked` intercepts every Unknown first and returns a blocked ABSTAIN, which `decide_verdict` also turns into a keep. Either way nothing is flagged. The `>=` in `reach_clears_dormancy` matches the gate's strict `<` exactly, so the boundary case is silent rather than wrong. | `2367a30` |
| `src/reaper/engine/policy_warnings.py:inspect` — int-truncation-diverges-from-the-season-path | `int(history_reach_days)` at the `progress_is_establishable` call cannot disagree with the season path. The value originates as `dormancy.history_reach_days()`, which returns an `int`; `policy._history_reach_days` only widens it with `float(...)`, and `season_scan` passes the same integer. Truncation is lossless on every production input, and toward-zero would warn MORE, not less. | `2367a30` |
| `src/reaper/engine/policy_warnings.py:inspect` — window-blocker-label-lookup-raises | The `BY_KEY[c.field].label` subscript in the new gate-off message cannot `KeyError`: `_protect_blocks_on_reach` returns `None` for an unknown key, so `window_blockers` only ever holds conditions with a resolved `FieldSpec`. `_join_and` is called on a provably non-empty list. | `2367a30` |
| `src/reaper/engine/policy_warnings.py:inspect` — withheld-overcounts-on-an-absent-watcher-count | The condemn-lane sum's premise holds: `evaluate_custom`'s graded arm treats an `Absent` input as EVALUATED, which would make the withheld total too high and the warning false — but no fact builder ever writes `Absent` to `distinct_watchers`. All three write Known or Unknown (`snapshot.py`, `season_scan.py`, `backtest.py`), and `facts_codec` thaws a missing key as Unknown. | `2367a30` |
| `frontend/src/components/PolicyEditor.tsx:ANCHORS` — flag-keep-conflicts-routes-nowhere | The `keep_last` anchor claims the field exactly (no competing anchor, no `prefix` matches it), `warningsAt` filters against the same `anchorClaims` the catch-all uses, and the `WarnBlock` sits inside the `mediaType === "tv"` subtree with the switch's row directly above it. Driven: the PolicyEditor suite passes 74, and the BOUND walk asserts the accessible description, so a dangling id would fail it. | `5c6b1ed` |
| `frontend/src/components/PolicyEditor.tsx:guardsHeld` — backend-emits-where-the-block-does-not-mount | The backend branch requires `media_type == "tv"` and the `tv` guard is `mediaType === "tv"`; the draft is re-seeded whenever the two disagree, so they cannot. The four extra backend conditions all narrow inside the mount, never outside it. | `5c6b1ed` |
| `frontend/src/components/Switch.tsx:describedBy` — sibling-prop-shape-diverges | `Segmented` and `QuantityInput` declare `describedBy?: string | undefined` byte-identically, and `exactOptionalPropertyTypes` is on, so `npm run build` type-checks every call site. `Switch` has no companion unit id to compose, so plain passthrough is right. | `5c6b1ed` |
| `frontend/src/components/GeneralPanel.tsx:removeKey` — new-announcement-overstates-the-fence | "Scripts and other apps can no longer use Reaper without signing in" describes the post-removal state, not what the key granted, so the fence being a route subset does not make it false. `remove_api_key` clears the digest, the middleware's key lane is the only header credential, and the image runs one worker. All four sibling copies agree with it. | `5c6b1ed` |
| `src/reaper/engine/policy_warnings.py:inspect` — hoist-changes-the-fourth-branch | Swept the old single `if` against the hoisted `mid_binge_holds_everything and reach_clears_dormancy and history_reach_days is not None` over media_type x keep_in_progress x reach x clears x hold: zero divergences. `progress_is_establishable` is two integer comparisons, pure, cannot raise, and the `is not None` guard still precedes the `int(...)`. | `5c6b1ed` |
| `src/reaper/engine/policy_warnings.py:inspect` — mid-binge-silencing-hides-an-operative-cause | The comment holds. `progress_unestablished` is the last arm of `_protection_reason` and returns a reason for every remaining content-bearing season, so `prunable` really is empty and `_detect_conflicts` iterates nothing. Both sides are fed from the same policy body and ask the same predicate with the same reach. | `5c6b1ed` |
| `src/reaper/services/season_pruning.py:plan_series_prune` — no-tv-season-overclaims | "No TV season will be flagged for removal" is exact, not an over-claim. `progress_unestablished` is the last arm of `_protection_reason` and returns a reason for every remaining content-bearing season, so `prunable` is genuinely empty for every series when the predicate answers False. | `2367a30` |
| `frontend/src/components/Fairness.tsx:Fairness` — divided-guard-is-dead-because-data-defaults | The `error && !data` guards added by the #190 sweep are not dead code. Every tested binding is raw `useQuery` output (`Fairness.tsx:193`, `GeneralPanel.tsx`'s `general` binding, `PolicyRuleEditors.tsx:276` and `:603`), so `data` is genuinely `undefined` until a first success. The `?? []` defaults in these files land on DERIVED names (`condemnAll`, `allFields`, `libOptions`), never on the binding the guard tests. Same answer for `!condemnVocab` and `!vocab`. | `0858711` |
| `frontend/src/components/ServiceModal.tsx:arrInstances` — some-every-pair-is-not-complementary | `.some((s) => instanceOptions(s.kind).length > 0)` and `.every((s) => instanceOptions(s.kind).length === 0)` are exact complements by De Morgan, and the enclosing arm already guarantees `seerrServices.data.length > 0`, so neither is vacuous over an empty array. The two branches cannot both fire and cannot both miss. | `0858711` |
| `frontend/src/components/StaleReadNotice.tsx:StaleReadNotice` — stale-line-sticks-after-recovery | The `error && data` stale lines cannot pin themselves on screen after a read recovers. `@tanstack/react-query` is `5.101.4` (`frontend/package.json:21`), and v5 clears `error` on a successful refetch; no query behind one of these lines uses `placeholderData` or hand-held state that would preserve it. | `0858711` |
| `frontend/src/components/NotInScanPanel.tsx:unread` — empty-report-misreported-as-unread | `unread = error && items.length === 0` does call a genuinely-empty-then-failed report unreadable rather than showing the all-clear. That is deliberate and fail-closed: an unread report and an empty one are indistinguishable at this component, which takes `items` as a prop, and claiming "nothing was left out" is the wrong direction to guess in. It also matches `dev` exactly, so it is not a regression. | `0858711` |
| `frontend/src/components/ReviewQueue.tsx:SeasonList` — division-leaves-an-uncovered-state | `if (!data)` after `if (isPending)` leaves no gap. A settled successful `useQuery` always carries data, so the only states reaching `!data` are pending (handled above) and error. Likewise the queue's stale line stacking over `tab.empty` reads as "this empty list may be stale", which is true; `dev` stacked a raw `error.message` over the same state. | `0858711` |
| `frontend/src/components/ReapPlan.tsx:ReapPlan` — reload-advice-drops-a-bulk-selection | Raised as three of #225's four sites: the plan loader, `ReapBreakdown`'s ledger refusal (via `ReapPlan.tsx:265`) and `NotInScanPanel`. All three are refuted by the component tree. `<main>` is a plain ternary on one `view` state (`App.tsx:620`, `:924-1010`) and `splitOpen` is a CSS class whose arms are each scoped to one view (`:642-644`), so exactly one section is mounted. These three live in the `view === "reap"` and `view === "fairness"` arms, and `ReviewQueue` in `view === "review"`, so navigating to any of them has already unmounted the queue and destroyed `selected` before the advice can render. Their reload advice costs nothing the queue owned. The fourth site, the reap sheet's loader, is NOT refuted: it renders outside `<main>` gated on `reapSheetRun` and was fixed. | `0858711` |
| `tests/test_repo_hygiene.py:_query_failure_handles` — counts-derived-from-the-source-they-check | The `_QUERY_FAILURE_HANDLES` and `_RELOAD_ADVICE` dicts are hand-written and compared with `==`, not generated from the walk. Replicating the walk against the branch reproduced 46 handles / 15 files and 14 reload hits / 9 files exactly, adding a handle moved the number, and an unseen `use*` in the direct spelling tripped the classification arm. The walk is also non-empty (75 shipped files) and skips on the REPO-relative path, so rule 145's gitignored-worktree trap does not apply. What WAS wrong is a different hole, fixed separately: a bag-returning hook. | `0858711` |
| `frontend/src/components/ServiceModal.tsx:save` — stale-prune-set-smaller-than-the-stored-map | The #204 fix prunes `plex_library_map` against `Object.keys(libMap)` when the folder read errored, and the worry is that this drops stored entries. It cannot: `libMap` is seeded from `instance?.plex_library_map ?? {}` (`:221`) and the prefill effect (`:245`) only ever ADDS (`if (!(f.path in next) && f.suggested_library)`), so the key set is always a superset of the stored map plus the operator's picks. `setFolderLibrary` writes `""` for an unmap, which the `if (chosen)` filter drops — the operator's own edit, saved deliberately. `serviceMap` (`:279`) is the same shape. | `05eaa30` |
| `frontend/src/components/ServiceModal.tsx:rootFolders` — iserror-clears-while-the-data-is-still-stale | There is no window where `isError` is false with the old list still in hand. React Query's fetch reducer resets `status`/`error` only when `data === undefined`, so a query holding data stays `status: 'error'` through every retry and leaves it only on a SUCCESSFUL fetch. The `isError`-keyed prune therefore cannot run against a stale list. | `05eaa30` |
| `frontend/src/components/PolicyEditor.tsx` — deleted-effect-leaves-validate-unrefreshed | Deleting the panel's own running->stopped effect loses nothing. `useScanSettled` is called at `App.tsx:801`, in the same component that renders `<PolicyEditor>` (`App.tsx:984`), so it cannot be unmounted while the editor is; both read one `["scanStatus"]` cache (`App.tsx:793`, `PolicyEditor.tsx:996`), so every observer sees the same transition in the same commit; and the shell polls it independently (1000ms running / 15000 idle) where the panel's idled at `false`. All three keys the effect invalidated — `["snapshot"]`, `["simulate"]`, `["validate"]` — are in `SCAN_SETTLED_KEYS`, and `["validate"]` prefix-matches the query's longer key. | `05eaa30` |
| `frontend/src/components/PlexPanel.tsx:switchServer` — cached-value-drives-a-write | Not another instance of #204's mechanism, though it reads like one. `verifyRef.current` is the certificate Switch's own on-screen value (seeded from `savedVerify`, then owned by the operator's clicks), and `verify_tls` is the field this control exists to write — the switch route needs it to PROBE the new server before saving. #204 was an unrelated field filled in from cache; this is the flag's own business. | `05eaa30` |
| `frontend/src/components/PlexPanel.tsx:done` — invalidation-overwrites-an-operator-draft | Adding `invalidateAllPlex` to the link and unlink paths cannot discard a draft (rule 39/53). All the keys feed reads, not drafts: the Leaving Soon switches are `checked={leavingSoon.data.…}` with no local state, the library switches are `checked={lib.enabled}`, `plex-resources` feeds pickers, `plexTrash` feeds a Reap-page count, and the web-address effect keys on the VALUE of `savedWebUrl`, so a refetch returning the same string does not re-seed the box. Three of the keys are also `enabled: linked`, hence disabled on every cancel/failure path. | `05eaa30` |
| `frontend/src/components/PlexPanel.tsx:saveVerify` — patch-payload-breaks-the-dirty-signal | Sending only `verify_tls` does not disturb the unsaved-changes guard. `webUrlDirty` compares `webUrl` to `savedWebUrl` and `seededFrom`, and the old call wrote the same stored address back, so `savedWebUrl` did not move then either; `SettingsNav`'s guard reads only `onDirtyChange`. `certDirty` requires `!linked` while `saveVerify` fires only when linked, so the two never interact. | `05eaa30` |
| `frontend/src/api.ts:put` — undefined-key-turns-reset-into-keep | `JSON.stringify` does drop a key whose value is `undefined`, but no caller of `setPlexSettings` ever passes one explicitly: `saveWebUrl` sends `{web_url: …}` and `saveVerify` sends `{verify_tls: next}`, and `PlexPanel.test.tsx` asserts `Object.keys(patch)` is exactly `["verify_tls"]`. All three server-side states are driven by `test_saving_one_plex_setting_leaves_the_other_alone`. | `05eaa30` |
| `frontend/src/components/PlexPanel.tsx:plexLinkPoll` — a-fourth-path-changes-the-linked-server | The "all three paths" claim holds. `plexSwitchServer` has one caller, `plexLinkPoll` is reached only from `PlexPanel` (`Login.tsx`'s `usePlexPinPoll` is the auth sign-in, a different flow), and `plexSetConnection` changes the address, not which server is linked. | `05eaa30` |
| `frontend/src/components/GeneralPanel.tsx:requestGenerate` — undefined-data-without-error-generates | The `fresh.isError === false && fresh.data === undefined` fall-through to `generate.mutate()` is not reachable as a defect. The button renders only past the panel's `isPending` and `!data` early returns, and a failed refetch keeps the last good `data` while raising `isError`, so the fall-through happens only with a fresh row whose `api_key_set` is false — which is the case that should generate. | `05eaa30` |
| `tests/test_repo_hygiene.py:_QUERY_FAILURE_HANDLES` — servicemodal-count-bumped-too-far | The 4 → 6 bump for `ServiceModal.tsx` is exact, not padding. The matcher counts at most one `isError` and one `error` per binding, so the two new save-handler reads take `rootFolders` and `seerrServices` from 1 to 2 each while `plexLibraries` and `arrInstances` stay at 1. | `05eaa30` |

| `src/reaper/services/watch_evidence.py:record` — func-max-is-the-aggregate-not-the-scalar | Two-argument `func.max` renders SQLite's scalar `max()`, not the aggregate. Verified against a real database: a lower reading does not lower the mark, a NULL reading does not erase one, a NULL mark takes the reading, and a higher reading raises both fields. The `case()` NULL arms are correct. | `b4e200c` |
| `src/reaper/services/watch_evidence.py:went_blind` — naive-aware-datetime-mismatch | `UtcTimestamp` is `EpochDateTime`: epoch ints in, tz-aware UTC out, and `from_epoch` is aware too. The SQL `max()` compares integers and the Python `<` never mixes kinds. A naive value would raise at bind time, not compare wrongly. | `b4e200c` |
| `src/reaper/services/watch_evidence.py:record` — duplicate-key-in-one-values-clause | SQLite refuses `DO UPDATE` against a duplicate key in one statement, but `readings` is a `Mapping`, so a key cannot repeat. | `b4e200c` |
| `src/reaper/services/season_scan.py:gather` — watch-marks-default-disables-the-tv-guard | OVERTURNED at this commit, and the row is kept because the reasoning read sound. The refutation was that the one production call site passes `watch_marks` and only tests omit it, so the `None` default is unreachable in production. True, and beside the point: the diff lane then DELETED the keyword at that call site and the whole suite stayed green (2929 passed), because every test drove the guard as a function. A default whose only protection is that one caller currently passes it is a rule 118 gap, not a closed one. `watch_marks` is now a required keyword-only argument on `gather` and `_judge_series`, so mypy refuses the drop. | `b4e200c` |
| `src/reaper/services/snapshot.py:scan` — degraded-history-read-mass-false-blind | A fall in the mirror's own counts is not reachable. The mirror is written `INSERT OR REPLACE` and never deletes; an empty mirror degrades un-plannably and a stale one degrades; and a Tautulli prune raises `HistoryRegressionError` before any write. So a degraded history read cannot make the whole library read blind at once. (The opposite direction, a mark raised too HIGH by a degraded scan, is NOT refuted and is issue #276.) | `b4e200c` |
| `src/reaper/services/watch_evidence.py:recall_all` — failed-mark-read-fails-open | No `except`: a failure aborts the scan rather than returning an empty map, so a mark that cannot be read cannot be mistaken for no mark. Fail-closed. | `b4e200c` |
| `src/reaper/db/models.py:WatchHighWater` — stale-mark-skips-a-safety-window | Rule 4/71 does not reach it: the mark is not a time-window clock, and nothing derives a deadline from it. A mark resurrected on a reused `media_key` reads as a fall, which holds the item — the keep direction. (That over-hold has no per-item exit and is issue #275.) | `b4e200c` |
| `src/reaper/services/watch_evidence.py:reading_for` — stored-mark-fires-when-the-key-is-none | An unmatched item takes no reading and makes no comparison, and `build_facts`'s no-key branch already gives all three watch facts `Unknown`. The docstring's reasoning for returning `None` rather than a zero is correct. | `b4e200c` |
| `src/reaper/services/snapshot.py` — movie-and-season-media-key-collide | `radarr:{instance}:{id}` against the four-part `sonarr:{instance}:{series}:{season}`. Disjoint by prefix and by arity, so the two lanes cannot overwrite each other's marks in the single `record` write. | `b4e200c` |
| `src/reaper/engine/signals.py:score` — the-new-unknowns-raise-condemn-pressure | The score is unsigned. All three flipped facts route to `UNREADABLE` (pressure 0, weight retained) and the graded keep on the all-time count takes its maximum discount. At shipped weights UNWATCHED 70 + FEW_WATCHERS 20 leaves coverage 0.10 against a 5000bp floor, and `base <= 100 x coverage` caps the score at 10. | `b4e200c` |
| `src/reaper/engine/verdict.py:decide_verdict` — a-blind-item-is-still-auto-condemned | `MinDormancyGate`, `DataHorizonGate` and `ServerPopularityGate` all return `blocked` on `Unknown`, so the item abstains. Only a hand reap passes, which is deliberate post-#96, and the reason reaches the panel through `_blocked`'s `"could not check ..."` prefix and survives freeze/thaw, so the simulator and the scan agree. (Whether the COUNT of such items may be called "held back" is a different question and was a real finding: all three gates are individually disablable.) | `b4e200c` |
| `src/reaper/api/plex.py:reset_watch_evidence` — unconfirmed-destructive-reset | Rule 73 governs a password-gated, content-bound confirm on a file deletion; this deletes no file and the next scan rebuilds the record. Every sibling destructive settings action is session-only with a log line and no password (`plex_unlink`, `clear_notifications`, `remove_api_key`), and this one adds a two-step confirm on top. Consistent. | `b4e200c` |
| `src/reaper/services/watch_evidence.py:forget_all` — racing-a-running-scan | The in-flight scan re-writes its own (blind, low) readings, which is exactly the baseline the operator just asked for. No inversion. | `b4e200c` |
| `src/reaper/services/watch_evidence.py:forget_all` — rowcount-under-reports-the-delete | Measured: an unfiltered `delete(WatchHighWater)` returns `rowcount == 7` for 7 rows on SQLite 3.50.4 / aiosqlite. The truncate optimization does not zero it, so the operator is not told nothing went when rows did. | `b4e200c` |
| `alembic/versions/20260730_1000_add_watch_high_water.py` — needs-the-reflection-guard | No rule 81 obligation. The heal migration `708192a3b4c5` sits BELOW both new revisions in the chain, so a database from the edited-baseline window is already reshaped before these run — matching the unguarded precedent immediately below them. Revision ids never changed on the branch, so a tester who ran an earlier build is stamped and re-upgrades to a no-op. | `b4e200c` |
| `src/reaper/db/models.py:WatchHighWater.media_key` — width-differs-from-its-siblings | All five `media_key` columns in `models.py` are `String(100)`. No truncation and no mismatch. `updated_at` is NOT NULL with no default, and `record` is its only writer and always supplies an aware `now`. | `b4e200c` |
| `frontend/src/components/PlexPanel.tsx:watchEvidenceStatus` — held-back-null-collapses-to-zero | It does not: `.scalar()` yields `None`, the model field is `int | None`, and the component branches `=== null` before `=== 0`. (The absence of any TEST for that three-state was a real finding and is now covered by four route tests plus the frontend table.) | `b4e200c` |
| `src/reaper/services/snapshot.py:build_facts` — widened-lane-outruns-the-mirror | Measuring from a play with no `added_at` is unclamped, but cannot exceed the reach: `history_sync._state` takes the horizon as `MIN(watched_at)` over ALL of `watch_event`, while `_watch_stats` takes `last_played` as `MAX(watched_at)` per key, so `last_played >= horizon` and `dormancy <= history_reach_days` by construction. The invariant `gates.py`'s reach-shortfall note and `policy.py:inspect` both rest on survives the new arm. | `40c20e0` |
| `src/reaper/engine/dormancy.py:reference_instant` — overload-1-gets-a-runtime-None | Only four callers exist in `src/`. Both resolving to the non-optional overload narrow first and cannot pass `None`: `backtest.facts_as_of` (`if item.added_at is None: return None`, no intervening rebind) and `calibration.derive` (`arrived = added_at.get(key); if arrived is None: continue`). `snapshot.build_facts` and `season_scan.build_season_facts` pass `datetime \| None` and resolve to overload 2. The overloads do not overlap incompatibly, since `datetime` is a subtype of `datetime \| None`; `mypy src/reaper` green. | `40c20e0` |
| `src/reaper/services/season_scan.py:build_season_facts` — walrus-restructure-changes-a-verdict | Swept the old four-arm chain against the new two-arm one over (`last_played` present/absent) × (`season_added_at` absent / pre-horizon / post-horizon) × (`watch_blind_reason` present/absent) = 12 cases, comparing observation kind, reference instant, and whether `scan.no_added_at` warns: zero divergences. The horizon-clamp case cannot differ, because the old arm passed `added_at=last_played` into `last_played or max(...)` and a `datetime` is always truthy, so the `max` was never evaluated. | `40c20e0` |
| `src/reaper/services/season_scan.py:build_season_facts` — walrus-leaves-reference-unbound | `if / elif (reference := …) is None / else` is one exclusive chain: the `else` is reachable only when the `elif` condition was *evaluated*, which is what binds `reference`. When `watch_blind_reason is not None` the chain exits at the first arm and neither later branch runs. `reference` appears at exactly two lines in the function, there is no enclosing loop, and mypy would reject a possibly-undefined read. | `40c20e0` |
| `src/reaper/services/season_scan.py:season_watch_stats` — ensure-schema-on-the-wrong-engine | `scan_runner` threads `cache_engine` into `snapshot.scan`, which forwards the same engine to `season_scan.gather` and thence to `season_watch_stats`. Same engine `history_sync.state` and `_watch_stats` use; the DDL lands on `cache.db`, never `reaper.db`. | `5c8078e` |
| `src/reaper/services/season_scan.py:season_watch_stats` — concurrent-ddl-race-with-the-movie-lane | The season lane runs as a spawned task overlapping the movie lane's `_watch_stats`, so two `ensure_schema` calls can interleave on one `cache.db`. `history_sync.ensure_schema` is already hardened for it: the fast path is read-only (PRAGMA + one `sqlite_master` lookup, no write txn), any rebuild serializes behind a process-level `asyncio.Lock` with an authoritative re-read inside it, and index statements are `IF NOT EXISTS` in sorted order. `scan()` also ensures the schema before spawning the season task, so the new call is a no-op read in production. | `5c8078e` |
| `src/reaper/services/snapshot.py:_fold_merged_watch_stats` / `src/reaper/services/fairness.py:_distinct_episodes` — unguarded-reader-reachable-before-a-sync | Each runs only after a guarded sibling in the same function, over a key set that is a subset of the sibling's, so a non-empty key set implies the guard already ran. (`backtest._plays` and `calibration.derive` genuinely lack the guard; they are unreachable and are filed as a question, not refuted.) | `5c8078e` |
| `tests/test_season_scan.py:test_no_arrival_date_but_a_play_measures_from_the_play` — a-test-that-cannot-fail | It does pass against the pre-change tree, because the old season lane already short-circuited on a truthy `last_played`. Refuted as a *defect*: it discriminates a correct restructure from a broken one, which is the property at risk in that diff, and its docstring says so outright ("on the lane that got it right"), meeting rule 118's bar that a non-discriminating test must not read as a proof. | `40c20e0` |
| `src/reaper/engine/dormancy.py:reference_instant` — none-does-not-actually-keep-the-file | Both dormancy gates fail closed on a non-`Known` input via `gates._blocked`, which returns a blocked ABSTAIN, and `decide_verdict` keeps the file on `blocked`. The docstring's "blocks both dormancy gates and abstains" is exact — note the mechanism is blocked-abstain, NOT PROTECT. | `40c20e0` |
| `docs/LEARNINGS.md` — measurement-instrument-is-unsound | The zero-across-every-stored-row measurement targets exactly the population whose behavior changes (matched item + missing `added_at`), and that state is genuinely reachable: `identity.Resolution.bound` sets `rating_key` and `plex_item` together, so the state requires a spine row carrying a key and no date. The `explanation_json` cross-check covers the rows `facts_json` is sparse on. (The docstring citing the *combined* row count for a movie-lane-only claim was a real finding, fixed at `a5381e5`.) | `40c20e0` |
| `docs/STATUS.md` — not-updated-for-a-behavior-change | No line in `STATUS.md` describes either lane's thaw, and the file's own rule is that closed work leaves it. Nothing there is now wrong. | `40c20e0` |
| `src/reaper/api/simulate.py:_season_guard_replay` — membership-checked-against-the-payload-not-the-plan | Fixed rather than refuted, and recorded so the shape is not re-raised: the check is now `set(plan.prunable) | {p.season_number for p in plan.protected}`, the set `guard_result` answers from. Both sets are policy-independent in their membership (`on_disk` decides who is in the union), so the union is the same under any draft. Closes #501. | `c18755c` |
| `src/reaper/api/simulate.py:simulate` — two-paths-answer-one-question | Issue #493, closed `Reviewed/Invalid` with timings in its thread and recorded here because a closed issue is read by nobody. Tier 1 is 275 ms of interaction latency avoided on a lane tier 2 answers exactly; the season lane's extra cost is ~0.13 ms/show, decode 0.4% of the request. The current two-tier shape stands and `tests/test_verdict_agreement.py` stays load-bearing. | `c18755c` |
| `src/reaper/services/season_evidence.py` — freeze-aborts-or-silently-skips-a-scan | The write runs inside the scan's own session, deduped on `judgment.group_key`, and `to_dict` emits nothing `json.dumps` refuses. Rows are swept by the FK cascade with `PRAGMA foreign_keys=ON`. No freeze failure can degrade a scan that would otherwise complete, and none can drop one show while keeping the rest. | `c18755c` |
| `src/reaper/services/season_evidence.py:missing_episode_map` — the-escape-hatch-previews-more-deletion-than-a-scan | An empty map makes `_anchor_positions` return a SUPERSET of the episode-precise answer, so `seq_protected` grows, `prunable` shrinks and `protected` grows. Rule 143 checked: the only protection defined over `prunable` is `_detect_conflicts`, and a larger `protected` means the seasons still in `prunable` draw MORE conflicts. Divergence is whole-lane and keep-direction (rule 31). | `c18755c` |
| `src/reaper/api/simulate.py:simulate` — a-field-the-spa-reads-arrives-undefined | All three `SimulationOut(...)` sites set `condemned_before` and `changed_titles` explicitly, and no route sets `response_model_exclude_unset`, so pydantic serializes the defaults regardless. `changed_titles` is a true superset of both deltas: every `newly`/`gone` increment sits inside a branch that also increments `changed`, checked arm by arm on both tiers. | `c18755c` |
| `frontend/src/components/PolicySimulator.tsx:STALE_HEADINGS` — an-unknown-refusal-renders-blank | `(staleKind && STALE_HEADINGS[staleKind]) ?? STALE_HEADINGS.gathers_differently` resolves `null`, `undefined` and an unknown id alike to the general heading, and `staleReason ?? <generic>` always renders a paragraph. Driven by a test passing a future id. Rule 142 swept: no consumer in either tree branches on the retired sentence's wording. | `c18755c` |
| `frontend/src/components/ScalesPanel.tsx:opener` — group-jump-seeds-a-term-that-matches-nothing | The premise that season rows carry no year is false: `season_scan.py:1901` sets `year=show_year` and `snapshot.py:1466` writes it (real snapshot: every season row carried both, none with a null `year` or `group_title`). So a seeded `"<show> <year>"` splits to stem + year and the second arm (`Candidate.year == year AND matches(stem)`) hits every season through `group_title`. A show with no year falls back to the plain-text arm. Same mechanism for a single-season item, whose Scales row takes `group_title or title` (`fairness.py:1173`). | `77a0c82` |
| `frontend/src/components/ReviewQueue.tsx:ReviewQueue` — seeded-search-fires-two-queries | The term seeds `useState` for BOTH `searchInput` and `search` (`:1407-1408`) and `search` is in the key (`:1547`), so the mount fires one filtered request. The debounce effect then sets the identical trimmed value and React bails. No unfiltered paint of the whole lane. | `77a0c82` |
| `frontend/src/components/ReviewQueue.tsx:ReviewQueue` — mount-seed-untrimmed-vs-effect-trimmed | Mount seeds `search` untrimmed while the effect trims, but the only producer is `titleWithYear`, which trims before returning. The two values are equal for every reachable input, so no query differs. | `77a0c82` |
| `frontend/src/components/ReviewQueue.tsx:ReviewQueue` — lane-change-remounts-and-reseeds | `App.tsx` passes no `key` to `ReviewQueue`, and the new lane's filters are adopted DURING the render. A lane switch re-renders rather than remounts, so the mount-time seed cannot re-fire on it. | `77a0c82` |
| `frontend/src/App.tsx:goTo` — collapsed-jumps-diverge-from-the-four-they-replaced | Truth table checked against all four originals. A non-review intent forces `lane = verdict`, collapsing the push condition to `intent.view !== view` (old `changeView`); `changeVerdict` is reachable only while on Review, collapsing it to `next !== verdict`; a nav click on the current tab pushes nothing and `setVerdict(verdict)` is a no-op, so B-23 survives; `onGoToReview` is reached only from Reap. (The focus NOT being restored by the undo was a real finding, fixed at `7087b45`.) | `77a0c82` |
| `frontend/src/App.tsx:goTo` — nonce-collision-drops-a-jump | `Date.now()` can repeat within a millisecond, but every producer is a human click on a page that unmounts the target view. No programmatic path emits two jumps in one tick. | `77a0c82` |
| `src/reaper/api/review.py:_like_literal` — unswept-like-sibling | Rule 72 sweep of `src/reaper/**` finds one other `LIKE`, `services/lists.py:666`, whose `:family` bind is always a module constant (`KEEP_TAG_SLUGS` / `PLEX_COLLECTION_SLUGS`, passed from `snapshot.py:2410/2418`). No operator text reaches it, and its wildcard is deliberate. `genre` uses a `json_each` bindparam and `library` an equality. | `77a0c82` |
| `src/reaper/api/review.py:_TRAILING_YEAR` — mis-split-loses-rows | Every arm ORs the full `matches(term)`, so the predicate is a strict superset of the old one: a zero-width separator (`Alpha1979`) or a mismatched paren only WIDENS. The anchor plus the `(?:1[89]\|20\|21)\d{2}` range rejects `20491`, `12345`, `Example 2345`. Bulk actions cover `shownKeys` only, so a widened search cannot sweep an unseen row into an override. | `77a0c82` |
| `frontend/src/format.ts:titleWithYear` — doubled-year-breaks-the-search | `carriesYear` recognizes only the `(YYYY)` spelling, so a stored title spelled `Example Show 2019` with year 2019 seeds `Example Show 2019 2019`. Reproduced against SQLite: the stem/year arm (`year == 2019 AND title ILIKE '%Example Show 2019%'`) still finds the row. | `77a0c82` |
| `frontend/src/components/ScalesPanel.tsx` — searched-chip-is-not-wired | Rule 25 checked: the chip the comment promises exists at `ReviewQueue.tsx:2356-2372`, gated by `filtering` (`:2001`), rendered first in the row, and clears in one click via `clearLabel`. | `77a0c82` |
| `frontend/src/components/ReviewQueue.tsx` — search-placeholder-has-an-ungenerated-sibling | Rule 144 sweep of `frontend/src`, `frontend/src/docs/content`, `docs/*.md` and `api.ts`: the sentence has exactly two copies, the input pair and the `list_candidates` docstring, and they agree on all three claims. No help-text or README third copy exists to drift. (The aria-label disagreeing with its OWN placeholder was a real finding, fixed at `f78cf20`.) | `77a0c82` |
| `src/reaper/services/retention.py:_compact_sync` — isolation-level-none-is-not-required | The comment calling `isolation_level=None` "required, not tidiness" is sound and the code is right. Measured both ways: on a default-isolation connection `VACUUM` raises `cannot VACUUM from within a transaction` after any DML, and succeeds after this function's PRAGMA-only statement mix — so the driver behavior the comment describes is real, and only its "required" is stronger than this one call site strictly needs. `services/backup.py:175` omitting the argument is the same PRAGMA-only case, not a counterexample. No operator loss, and the defensive choice survives a statement being added above the vacuum later. | `5139d20` |
| `src/reaper/services/retention.py:_doomed` — not-in-limit-subquery-or-null-run-id | Two candidates, both dead. SQLite accepts `LIMIT` inside a `NOT IN` subquery and SQLAlchemy emits it correctly (compiled and executed). A NULL `ReapRun.snapshot_id` would make the whole `NOT IN` unknown and sweep nothing — the keep direction, and unreachable anyway: the column is `Mapped[int]` (`models.py:809`) and `nullable=False` in the frozen baseline (`:217`). | `5139d20` |
| `src/reaper/services/retention.py:compact_if_fragmented` — vacuums-or-creates-the-wrong-file | `data_dir / "reaper.db"` matches `config.py:220`'s `database_url`, and `main.py:210` passes `settings.data_dir` into `build_scheduler`. A raw `sqlite3.connect` on a missing path does create a 0-byte file, but `PRAGMA page_count` then returns 0 and `if not pages: return False` catches it — and the caller only reaches compaction on a database it just swept. (The *unpinned* `data_dir` argument was a real test gap, fixed at `e6d1d3d`.) | `5139d20` |
| `src/reaper/services/retention.py:sweep_old_snapshots` — batch-loop-miscounts-or-overflows | `removed += len(doomed)` sits after the batch commits and the early `return` fires only on an empty batch, so no path double-counts or undercounts a committed batch. Rule 94 does not bind: `doomed` is capped at `SWEEP_BATCH` = 10 and nothing else expands a bindparam. The cascade is DB-level rather than ORM, and one batch of 10 snapshots × several thousand candidates over a 297 MB database takes 0.11 s — `UniqueConstraint("snapshot_id", "media_key")` gives it a covering index. | `5139d20` |
| `src/reaper/services/scheduler.py:SNAPSHOT_SWEEP_JOB_ID` — unschedulable-job-leaks-or-404s | Nothing enumerates scheduler jobs generically: `get_schedule` iterates `SCHEDULABLE_JOB_IDS` (`api/settings.py:748`), `set_job_schedule` and `run_job` 404 on anything outside it, `apply_stored_schedules` iterates `MAINTENANCE_JOB_IDS`, and `frontend/src` hardcodes no job id. Exactly the `SESSION_SWEEP_JOB_ID` precedent rule 55 points at, mirrored test included. Also checked and clean: the `scheduler`/`retention` name collision cannot recurse (the call is module-qualified), `utcnow()` is tz-aware and `IntervalTrigger` takes its zone from `start_date`, `job_defaults` already carry `max_instances=1`, and `except Exception` cannot swallow `CancelledError` in 3.13 (rule 128). | `5139d20` |
| `src/reaper/api/runs.py:reap_in_flight` — running-flag-stuck-true-starves-compaction | A reap wedged by #327 leaves the *run row* `EXECUTING`, not the in-memory flag: `status.running = False` sits in `_reap`'s `finally`, which runs on `CancelledError` too, and `execute_run`'s deliberately bare `except Exception` clears it on every synchronous refusal before the task exists. So the predicate cannot answer True forever and starve the vacuum. | `e4d8e9d` |
| `src/reaper/api/runs.py:reap_in_flight` — reap-mid-write-while-the-flag-is-false | No such window. `running` is set True synchronously before `asyncio.create_task`, and cleared only in the `finally`, which is reached after the run session has already committed. Every reap write is inside the flag. | `e4d8e9d` |
| `src/reaper/api/runs.py:reap_in_flight` — predicate-raises-into-the-sweep | `getattr(app.state, "reap_status", None)` absorbs the `AttributeError` Starlette's `State` raises, and `status.running` is a pydantic field that always exists. No path raises; if one did, the job's `except Exception` skips the compaction, which is the safe direction. | `e4d8e9d` |
| `src/reaper/services/scheduler.py:build_scheduler` — reap-predicate-lost-on-a-rebuild | The predicate is required and keyword-only, so a missed call site is a boot-time `TypeError`, never a silent placeholder. One production caller (`main.py:208`), and nothing re-wires the job afterwards: `apply_stored_schedules` iterates `MAINTENANCE_JOB_IDS`, which excludes `SNAPSHOT_SWEEP_JOB_ID`, and `_maintenance_specs` holds only the three cron jobs. | `e4d8e9d` |
| `src/reaper/services/scheduler.py:sweep_old_snapshots` — persisted-job-replays-with-two-args | No jobstore is configured, so APScheduler defaults to `MemoryJobStore` and every job is rebuilt at boot; nothing is pickled, and the non-picklable lambda arg is safe. A short `args` list would be rejected by `check_callable_args` at `add_job` anyway. | `e4d8e9d` |
| `src/reaper/services/scan_runner.py:scan_running` — frozen-import-binding-or-leaked-flag | `scheduler.py` imports the module (`from reaper.services import scan_runner`) and calls `scan_runner.scan_running()`, so it reads the global at call time rather than freezing it. `run_scan` claims the flag and releases it in a `finally`, so exceptions and cancels both clear it, and the whole gather plus the single terminal commit sit inside the claim. | `e4d8e9d` |
| `src/reaper/services/retention.py:compact_if_fragmented` — ungated-vacuum-sibling | Rule 72 sweep of every `VACUUM` in `src/`: the only other is `backup.py`'s `VACUUM INTO`, which reads `reaper.db` into a new file rather than rewriting it, so it never takes the write lock and owes no gate. `restore.apply_pending_restore` runs from `preflight.py` before the app boots. The "only caller" claim in the docstring greps true. | `e4d8e9d` |
| `src/reaper/services/scheduler.py:sweep_old_snapshots` — skip-reopens-the-unbounded-growth-of-#315 | The gate skips the compaction only; `retention.sweep_old_snapshots` runs unconditionally *before* it is evaluated, so rows stay bounded and the file cannot pass its prior high-water mark. Only the return of already-freed disk is deferred. (That the deferral could be *permanent* was a real finding, fixed at `6f1046e` with a jitter on the interval.) | `e4d8e9d` |
| `src/reaper/services/executor.py:execute` — journal-halt-return-skips-the-finally | `terminal` is assigned before the `return` in the `except _JournalWriteError` arm, and Python runs `finally` on the return path, so `_commit_and_finalize` writes ABORTED. Read back through a fresh session by the class's own tests. | `bbb6315` |
| `src/reaper/services/executor.py:_revive` — reap-run-row-never-reloaded | Nothing reads `run`'s attributes after the point a rollback can happen: `run.approved_at` is evaluated as an argument before the try, and the `finally` only *sets* columns, which does no IO on an expired instance. Proven by probe in all three lanes. | `bbb6315` |
| `src/reaper/services/executor.py:_revive` — lazy-relationships-stay-expired | `_Delete.steps` is a tuple built in `execute()` and `_Delete.candidate` a direct object reference; every attribute read on either is a mapped column, and `AsyncSession.execute` prebuffers, so the two SELECTs repopulate them. No relationship is involved. | `bbb6315` |
| `src/reaper/services/executor.py:_JournalRow.replay` — stale-value-written-over-a-newer-one | `_mark` setattrs only after its commit lands, so the ORM row equals durable-plus-staged at every capture point; within an item every committing `_mark_*` precedes every in-memory `_fail`/`_stage_*`/`_mark_skipped`. | `bbb6315` |
| `src/reaper/services/executor.py:_run_deletes` — journalled-false-and-the-run-continues | Exactly two exits per iteration, and both check: the SKIPPED branch before its `continue`, the acted-on branch before the canary and halts-run checks. (That the second had no *test* was a real finding, fixed here.) | `bbb6315` |
| `src/reaper/services/executor.py:_commit_and_finalize` — empty-write-list-does-nothing | `write=[]` reduces to a bare `session.commit()` that flushes the staged journal writes, as the docstring says. `terminal` is None only for a BaseException that is not `CancelledError`. | `bbb6315` |
| `src/reaper/services/executor.py:_run_deletes` — approved-size-hoisted-above-the-commit | Candidate rows are frozen per snapshot and never mutated by the executor, so `delete.candidate.size_bytes` read before the commit is the identical value the old code read at `report.deleted_bytes += freed`. | `bbb6315` |
| `src/reaper/services/executor.py:_send_for_real` — journal-write-error-swallowed-by-a-handler | Walked every handler between the `_mark_*` call sites and `execute()`. `_send_movie`, `_send_season`, `_one_delete` and `_run_deletes` carry none; `_send_for_real`'s catch-all is preceded by the explicit re-raise, and deleting that re-raise fails a test. The broad handlers in the item path wrap Plex and Tautulli reads only. | `bbb6315` |
| `src/reaper/services/executor.py:_commit_and_finalize` — terminal-update-has-no-state-guard | The PLANNED-to-EXECUTING claim in `execute()` is the guard and there is no second writer; a `WHERE state = ...` here could only ever decline to record a terminal state, which is the wedge itself (rule 26). | `bbb6315` |
| `src/reaper/services/executor.py:_commit_journal` — dead-branches-in-the-attempt-loop | All three reachable: `if attempt == 2: break` skips a pointless rollback on the last attempt, `reap.journal_recovered` fires whenever attempt 2 succeeds (observed live), and `reap.journal_unrecoverable` is reached on every failing path. | `bbb6315` |
| `src/reaper/api/runs.py:execute_run` — swallowed-trailing-commit-loses-a-write | The terminal row is a committed Core UPDATE and the journal is committed per item, so the only thing that commit still flushes is the `run.state`/`aborted_reason`/`finished_at` assignment in the `finally`, duplicating values already on disk. | `bbb6315` |
| `src/reaper/services/executor.py:_finalize_plex` — runs-on-a-dead-session | It reads no ORM state at all, only the plain `_affected_sections`, `_deleted_by_section`, `_section_pre_counts` and `_section_titles`, so a dead session cannot reach its three interlocks. | `bbb6315` |
| `src/reaper/api/runs.py:execute_run` — stale-run-serialized-into-a-response | No production caller holds `run` past `execute()`. The dry-run route never enters the `finally`'s `not dry_run` branch, and the execute route's session is closed immediately after; `GET /runs/{id}` and `GET /runs` use fresh request sessions. | `bbb6315` |
| `src/reaper/services/executor.py` — journalled-british-spelling | Not a diff finding: 47 instances across `src/`, `frontend/` and `docs/` predate this change and there are zero of the American spelling, so the new identifiers are the consistent choice. A repo-wide sweep, if anyone wants one. | `bbb6315` |
| `src/reaper/services/season_evidence.py:from_dict` — codec-loses-a-field-on-the-round-trip | All 15 `SeasonPruneInput` fields and all 6 `SeasonStats` fields survive. Every int-keyed map is `str()` on the way out and `int()` on the way back, including both nested ones (`progress_by_user`, `last_play_by_user`); the `None` / `{}` three-state is preserved on the one field that has it; `size_on_disk=None` stays `None` rather than becoming `0`; tuples become lists the planner re-`set`s. Two import-time drift guards (`_KEYS`, `_SEASON_FIELDS`) fail the process on a field added later, and `_SEASON_FIELDS` is the sharper one because a `SeasonStats` field WITH a default would otherwise thaw as that default in silence. | `221b92d` |
| `src/reaper/services/season_evidence.py:_epoch` — truncation-expires-a-viewer-the-scan-kept | Only `now` carries sub-second precision: `last_watched_by_user` and `last_play_by_user` come from `from_epoch` over integer mirror epochs. Flooring `now` moves `active_progress`'s cutoff *earlier*, which keeps a viewer the scan expired rather than the reverse (rule 31). The scan itself never round-trips the codec — it plans from the in-memory `SeasonPruneInput`. | `221b92d` |
| `src/reaper/engine/policy.py:PolicyBody.evidence_hash` — season-fields-leaving-the-hash-reach-a-deletion | `evidence_hash` is read at exactly one site in `src/` (`simulate.simulate`). The nine season fields stay in `scoring_hash` and in `policy_hash`, so a season edit always moves the hash rule 113 enforces at execute time, and can never land on tier 1's stored-verdict path. | `221b92d` |
| `src/reaper/services/season_scan.py:_judge_series` — removing-the-fully-kept-skip-narrows-scan-protection | `_protection_reason` evaluates incomplete / airing / first-season / keep-last *before* `seq_protected`, so a show the offline pass found fully protected stays fully protected whatever the newly-read episode map says. The scan's own plan is argument-for-argument identical to the pre-change inline call. | `221b92d` |
| `alembic/versions/20260803_1200_add_season_prune_evidence.py` — migration-not-additive-or-not-chained | `down_revision` is the current single head (`f708192a3b4c`), the upgrade is a pure `create_table`, and `alembic upgrade head` from the frozen baseline produces a schema matching `Base.metadata.create_all`. The FK cascade is pinned by `test_retention.py::test_the_frozen_season_evidence_goes_with_it_too`. | `221b92d` |
| `src/reaper/api/schemas.py:SimStale` — a-refusal-the-panel-has-no-branch-for | The enum has three members, `STALE_HEADINGS` is a `Record<SimStale, string>` so a gap is a tsc error, and `test_the_browser_knows_every_refusal_the_server_can_send` pins the union against the enum. An id the build does not know falls back to the general heading and still renders the server's sentence, which is covered by a test passing a future id. | `221b92d` |
| `docs/STATUS.md` — not-updated-for-a-behavior-change | No STATUS line describes the simulator's refusal scope, so none is now wrong. The two sibling simulator PRs (#487, #490) did not touch it either. | `221b92d` |
| `src/reaper/services/lists.py:PlexWatchlist` — empty-read-wipes-the-stored-membership | No trigger. Unlike `ArrTagRule` / `PlexCollection` there is no missing-container state and no floor, but plexapi's `fetchItems` pages to `totalSize` and raises on auth failure, so the only empty this can see is a genuine one. "The operator cleared their watchlist" is the case the class docstring accepts deliberately. | `f74c0c8` |
| `src/reaper/services/snapshot.py:build_facts` — dropping-the-hard-mode-membership-filter | No-op. Nothing in `src/` ever writes `mode='soft'`; `_run` passes `mode=ListMode.HARD` unconditionally, so the filter selects everything it is given. | `f74c0c8` |
| `src/reaper/services/lists.py:IMDB_PRESETS` — a-truncated-mirror-shrinks-the-protection | A custom `ls…` list floors at 1 where the Top 250 floors at 200, but a partial JSON array from a single non-paged GET is not a reachable state: the body parses or it does not. | `f74c0c8` |
| `src/reaper/services/season_scan.py:build_season_facts` — a-defaulted-memberships-argument-makes-on-lists-absent | Latent, not live. The only production caller (`_judge_series`) passes it; the default `()` is reachable from tests alone. | `f74c0c8` |
| `src/reaper/services/list_rules.py:rename_list` — a-repaired-policy-strands-rules-on-the-old-name | Self-healing. While `lists_migrated` is the repair, `convert_list_protections` re-resolves every rule target from the live registry on each load, so the rules follow the rename anyway. The other repair arms (`rescaled`, `rating_rules_recovered`) cannot coexist with a body already carrying `on_list` rules. | `f74c0c8` |
| `src/reaper/services/list_config.py:_refuse_name_twice` — non-ascii-case-variants-share-one-keep-rule | SQLite's `func.lower` and the `NOCASE` collation are both ASCII-only where Python's `casefold` is not, so two non-ASCII case-variant names can coexist. Real, and no plausible operator trigger; rule 88's ASCII path is intact. | `f74c0c8` |
| `src/reaper/services/list_rules.py:_save` — appending-a-policy-row-whose-hash-already-exists | `Policy.policy_hash` is indexed, not unique, and the docstring states that reverting appends a duplicate hash on purpose. | `f74c0c8` |
| `src/reaper/services/snapshot.py:sync_protection_lists` — a-keep-tag-retire-fires-when-an-arr-is-unreachable | `build_sources` constructs clients from DB rows without connecting, so an unreachable instance still contributes its slug to `keep_tag_slugs`; `_retire`'s `current & failed` guard then stands the sweep down when the sync itself raised (rule 115). | `f74c0c8` |
| `src/reaper/engine/policy_migrations.py:convert_list_protections` — the-whitelisted-gate-is-stripped-with-no-collection-list | Migration `20260803_1900` seeds a `plex_collection` row into a table `20260803_1800` created empty in the same chain, so `own` is non-empty on every real upgrade. The name-collision early return is unreachable outside a branch-dev database. | `f74c0c8` |
| `frontend/src/components/ListsPanel.tsx:ago` — a-naive-timestamp-read-as-local-time | `ListHealth.last_success` goes through `clock.from_epoch`, which returns `datetime.fromtimestamp(seconds, tz=UTC)`, so the wire carries an offset. | `f74c0c8` |
| `frontend/src/components/ListsPanel.tsx:libraries` — a-disabled-query-renders-loading-copy-forever | `usePlexLibraries({enabled: view === "form" && source === "plex_collection"})` is read under exactly that condition, so `isPending` is never displayed on the off branch. | `f74c0c8` |
| `frontend/src/components/ListModal.tsx:removeList` — a-204-body-parsed-as-json | `parseBody` short-circuits on 204 before `response.text()`, so `del<void>` returns undefined cleanly. | `f74c0c8` |
| `frontend/src/components/ListModal.tsx:invalidate` — a-policy-invalidation-that-misses-a-media-type | react-query matches by key prefix, so invalidating `["policy"]` covers `["policy", "movie"]` and `["policy", "tv"]` both. | `f74c0c8` |
| `frontend/src/components/PolicyRuleEditors.tsx:KeepRulesEditor` — an-always-disabled-vocabulary-query | `on_list` is a PROTECT-lane field with no `media_types` restriction, so it is always in the served vocabulary and the query is never disabled. | `f74c0c8` |
| `src/reaper/api/simulate.py:simulate` — an-on-list-rule-crashes-the-policy-probe | The probe is signal-only (`kind: "signal"`, one call site at `PolicyEditor.tsx:928`), so a PROTECT-lane field never reaches `preview._bare_facts`. | `f74c0c8` |
| `alembic/versions/20260804_1400_list_name_unique_nocase.py:_disambiguate` — a-rename-withdraws-the-later-row-s-cover | It does, but `list_config` is created by this same PR, so the only reachable population is a tester who ran an intermediate branch build. Not a defect against any released install. **Superseded at `PR #492`: fixed anyway rather than left declined, because the fix is a verdict-preserving rule copy and the branch-window population is exactly the testers running on real data.** A later pass raised it again from the other end (`sync_rule_names` rewriting membership to a spelling no rule names), which is what a row refuted on population alone invites; read this line before re-refuting one. | `f74c0c8` |
| `alembic/versions/20260804_1300_heal_list_config_shape.py` — a-batch-recreate-drops-the-unique-constraint | Refuted by dumping the final DDL: `name VARCHAR(100) COLLATE "NOCASE" NOT NULL` with `CONSTRAINT uq_list_config_name UNIQUE (name)` intact. | `f74c0c8` |
| `frontend/src/components/ListsPanel.tsx:check` — two-rows-checking-concurrently | A row-scoped check leaves other rows' buttons live, but a narrowed pass sweeps only the definition it checked, on a slug pattern anchored to that definition's id (`lists.list_slugs`), so two concurrent checks act on disjoint rows and neither can switch the other's protection off. Re-argued at `PR #492`: the original reason was that `only=` retires nothing at all, which stopped being true when the narrowed pass gained its own sweep. | `f74c0c8` |
| `src/reaper/services/list_rules.py:rename_list` — stranded-rules-after-a-degradation-directed-save | The `f74c0c8` refutation above holds and `b35858a` strengthened it: `convert_list_protections` re-resolves rule targets via `_conversion_list_names` (`profiles.py:295`) on every load including the editor-draft GET, so a draft saved after a rename already spells the new name; migration `d5e6f7a8b9c0` persists the conversion at upgrade, shrinking the repaired window to the declined corner; `RESCALED` / `RATING_RULES_RESTORED` bodies cannot coexist with stored `on_list` rules. The residual `FELL_BACK` corner is not opened by the skip — writing a rename through the fallback is the rule-65 substitution the skip forbids, and the scan is degraded for that whole window. | `b35858a` |
| `src/reaper/services/profiles.py:_conversion_list_names` — plex-libraries-read-failure-widens-deletion | The best-effort `plex_libraries` read falling back to `[]` was raised as a rule-65/91 fail-open. It is not: an empty library map makes `own_list_media_scope` return BOTH for every collection, which only ever ADDS a collection's rule to a policy it would otherwise be scoped off. Losing this read widens PROTECTION, never what can be reaped, so it is the keep direction (rule 28's add-only-evidence shape), not the condemn direction rule 65 guards. The registry read on the same path still raises and degrades. | `a51fd58` |
| `src/reaper/engine/policy_migrations.py:convert_list_protections` — collection-scoping-desyncs-the-policy-hash | Scoping makes `protect_conditions` a function of `plex_libraries`, and `policy_hash` covers it (rule 113), but no two paths compute a divergent hash for one install at one instant: the scan stamp and the executor's `live_policy_hash` both derive the converted body through `active_policy`/`active_policies` from the same read; the migration persists the converted body, after which `has_legacy_list_protections` is False and the shim never re-runs, removing `plex_libraries` from the hash for migrated installs. The only residual is temporal — a `plex_libraries` change between plan and execute on an un-migrated body refuses the run, fail-closed. | `a51fd58` |
| `src/reaper/engine/policy_migrations.py:own_list_media_scope` — ascii-name-collision-mis-scopes | An ordinary same-named collection in a movie and a TV library cannot both exist: `list_config.name` is NOCASE UNIQUE and for pure-ASCII names SQLite NOCASE folds identically to `casefold`, so the second save is refused. A single collection lives in one library of one kind, so dropping its other-type rule withdraws nothing. The one constructible hole was a NON-ASCII case pair, which NOCASE and the ASCII `func.lower` pre-check admit as two rows — that is CONFIRMED and fixed at `0651278` by unioning the types; the ASCII case is refuted. | `a51fd58` |
| `scripts/mutation_scope.py:probe` — broken-probe-yields-a-false-clean-report | A zone whose probe source cannot import does NOT report a defended gate layer. `probe()` returns the last stderr line as a string, `run_one` calls it for the BASELINE before mutating anything, and `isinstance(baseline, str)` raises `SystemExit(f"baseline {baseline}")` — so the run dies loudly with the `ImportError` in the message and generates no mutants at all. The per-mutant call is guarded the same way and lands in `m.direction` as the failure string. The underlying gap was real and is fixed here: `engine-gates` named `CuratedListGate` and `WhitelistGate`, retired from `gates.py` at `c3f8c44`, so the zone could not be run; the probe now imports and yields 60 cases, driven. What is refuted is the silent half. | `552b427` |
| `alembic/env.py:include_name` — dropping-cache-tables-re-admits-a-drop-table | The `CACHE_TABLES` arm was inert, so removing it changes nothing. All six tables are raw DDL executed against the engine from `db/session.create_cache_engine` (`services/imdb_dataset.py:214`, `services/lists.py:680`, `services/history_sync.py:113`), which points at `cache.db`; Alembic is pointed at `sync_database_url` = `reaper.db`. A long-lived real `reaper.db` holds none of the six, and `alembic check` is green against a DB freshly upgraded to `e6f7a8b9c0d1`. | `73dac25` |
| `alembic/env.py:include_name` — wrong-parent-names-key-for-column-and-fk | `parent_names.get("table_name")` is the documented key for both arms in the pinned alembic 1.18.5: `autogenerate/compare/tables.py:256` calls `run_name_filters(c.name, "column", {"table_name": tname, "schema_name": schema})` and `compare/constraints.py:652` the same shape for `"foreign_key_constraint"`. Returning `True` for every other `type_` restores alembic's default (no filtering), which is what the removed table/index arms are being reverted to. | `73dac25` |
| `alembic/env.py:run_migrations_offline` — deleting-offline-strands-an-invoker | No `--sql` invocation exists in the tree. `docker-entrypoint.sh`, `scripts/dev-local.sh:296`, `.github/workflows/ci.yml:249` and `scripts/baseline_capture.py:318` all run `alembic upgrade head` online, and the only reader of `context.is_offline_mode()` was the deleted branch itself. Nothing outside `env.py` referenced the function; `tests/test_migrations.py`'s parametrized invoker went with it. | `73dac25` |
| `alembic/versions/20260808_1200_release_m_for_six_retired_columns.py:upgrade` — a-batch-rebuild-drops-an-index-or-constraint | Dumped `sqlite_master` for all four tables at `d5e6f7a8b9c0` and again at `e6f7a8b9c0d1`: every PK, UNIQUE, autoindex, named index and the `fk_profile_active_policy_id_policy` foreign key survive, and `list_config.name` keeps `COLLATE "NOCASE"` because the block restates it. `sa.false()` renders as `DEFAULT 0` and an INSERT omitting `enabled` / `built_in` succeeds. The **downgrade** is a different answer — see the confirmed finding. | `73dac25` |
| `src/reaper/db/models.py` — a-surviving-read-of-one-of-the-six-retired-columns | Swept `src/`, `tests/`, `frontend/src/`, `scripts/` and `alembic/` for all six names. Every surviving `poster_url` is the derived `/api/poster/{rating_key}` value built in `api/review.py`'s `_candidate_out` and `services/fairness.py:1178`, never the stored column; `PlexPin.code` still feeds `plextv.py:99`'s `auth_url`, so the stored `pending_plex_login.pin_code` had no reader; `plex_server.owner_plex_account_id` is unrelated to `AppUser.plex_account_id`, which auth still uses. No `Profile.policy` relationship load, no `order_by`/`where` and no raw SQL over any of the six outside the migrations. | `73dac25` |
| `alembic/env.py:include_name` — hiding-a-column-breaks-a-later-batch-rebuild | Batch mode reflects with `Table(self.table_name, m1, autoload_with=...)` (`alembic/operations/batch.py:140`), which never consults autogenerate's name filters — those live on `AutogenContext`, not on reflection. Demonstrated: the release-M `profile` rebuild copied both hidden columns into the new table. | `73dac25` |
| `alembic/versions/20260808_1200_release_m_for_six_retired_columns.py:upgrade` — profile-rebuild-cascades-the-autonomy-grants-away | A batch rebuild is `CREATE tmp` / `INSERT SELECT` / `DROP TABLE profile` / `RENAME`, and with `PRAGMA foreign_keys=ON` SQLite's `DROP TABLE` runs an implicit `DELETE` that would cascade into `autonomy_grant.profile_id` (`ondelete="CASCADE"`). It cannot fire: the pragma is set by `_configure_sqlite`, registered per engine in `db/session.create_engine`, and `alembic/env.py:131` builds its own engine with `engine_from_config`, which never gets that listener — so foreign keys are OFF for the whole migration. Demonstrated on a populated database: the grant row survived the `profile` rebuild intact. | `73dac25` |
| `src/reaper/services/profiles.py:active_policy_row` — retiring-active-policy-id-changes-the-policy-in-force | Deleting `Profile.active_policy_id` and the `Profile.policy` relationship changes no resolution, because neither was ever consulted. "Active" has always been pure recency (`profiles.py:150`, newest `policy` row per `media_type`), and a sweep of `src/` finds zero reads of the attribute or the relationship outside the write that set it. The real defect on this path is a different mechanism — `_ensure_active_policy_row` persisting the *bare* `DEFAULT_MOVIE_POLICY` — and is filed as a confirmed finding, not refuted here. | `73dac25` |
| `src/reaper/api/middleware.py:api_key_scope_description` — keep-list-fence-copy-outlived-the-deleted-read | Rule 144's failure does not fire on deleting `GET /api/whitelist`. Every sibling sentence was read: the generated auth box and `api_key_refusal_detail` enumerate the four denied-read *phrases* and never named the keep list; `GeneralPanel.tsx`'s hand-written API key help says "reads nearly everything" with no keep-list clause; `manual/operating/security.mdx:33` says a key "cannot spare a title or add one to the whitelist", which the deletion makes *more* true, not less. The change removes a reachable read and two already-refused writes, so every ungenerated copy moves in the safe direction. No copy claimed a key could read the keep list. | `d40aa0f` |
| `src/reaper/api/whitelist.py:_resolve_title` — both-write-routes-phrase-went-stale | The docstring's "an API key is refused both write routes outright" survives the deletion. Before, "both" was `POST /api/whitelist` and `POST /api/override` (the two callers of `_resolve_title`); after, the module holds exactly two write routes, `POST /api/override` and `DELETE /api/override/{media_key}`, and `_api_key_allowed` returns False for both (pinned at `test_general_and_logs.py:701-702`). The sentence reads true on either parse, and the claim it guards — "nothing reaches the refusal today" — is strictly more true with one fewer caller. | `d40aa0f` |
| `tests/test_api_type_mirror.py:EXPECTED_INTERFACES` — hand-reconciled-counter-did-not-move-with-its-population | Deleting `SpareIn` from `api/schemas.py` moves neither counter, and neither should have moved. `EXPECTED_INTERFACES` counts `export interface` declarations in `frontend/src/api.ts` above `CLIENT_MARKER`; `EXPECTED_PAIRS` counts the subset of those that pair to a server model. Both populations are browser-side. `SpareIn` was request-shaped and had no TS interface (the deleted `spare`/`unspare` methods sent inline object literals), and no `ALIAS` entry or `CLIENT_ONLY` member referenced it — grepped `SpareIn` across `frontend/`, `tests/` and `src/`, zero hits outside the deleted lines. The mirror walks browser→server only, so an orphaned server model is invisible to it by construction. 171 tests green. | `d40aa0f` |
| `tests/test_whitelist.py:_stored_entries` — replacement-helper-reads-different-rows | The new test-local helper is byte-identical to the deleted `whitelist.list_spared`: same `select(WhitelistEntry).order_by(WhitelistEntry.created_at.desc())`, same `list(rows.scalars().all())`. Its two callers previously drove `whitelist.spare(...)`, a pure delegation to `set_override(..., decision="spare")`, and now call `set_override` directly with the same arguments, so both the rows read and the assertions on them are unchanged. | `d40aa0f` |
| `tests/test_override_truth.py:test_clearing_one_records_what_it_used_to_be` — de-parametrizing-dropped-a-branch-driver | Removing the `["/api/override", "/api/whitelist"]` parametrize loses no coverage. `unspare_item` and `clear_override` were line-for-line the same body, so the second id drove the same handler at a second URL; the handler, the override log assertion and the `{"removed": …}` body are all unchanged in the surviving single-route form. Grepped the suite for another driver of `unspare_item`: none, because the function is deleted. Same for `test_clearing_nothing_says_nothing`. | `d40aa0f` |
| `src/reaper/api/whitelist.py:list_whitelist` — supply-chain-left-a-dangling-reference | Rule 64's sweep comes back clean for the *route* references. Grepped `api/whitelist`, `SpareIn`, `list_spared`, `spare_item`, `unspare_item`, `list_whitelist` and `whitelist.spare` across `src/`, `frontend/src/`, `tests/`, `manual/`, `website/`, `.env.example` and `docs/`: no client method, no query key (`useOverrideMutations.ts` invalidates `["candidates"]` and `OVERRIDE_AWARE` only), no prop, no TS interface, no OpenAPI tag entry. The served schema drops `SpareIn` from `components.schemas` and the three paths; `test_openapi_tags.py` is green and no tag empties. The one docstring that *does* still name the removed surface is reported as a separate finding. | `d40aa0f` |
| `tests/test_grace.py`,`test_reap_loop.py`,`test_review_reap.py`,`test_whitelist.py` — script-rewrite-silently-changed-a-call-argument | All 32 `whitelist.spare(...)` → `whitelist.set_override(..., decision="spare", ...)` rewrites are argument-identical, proved rather than read: an `ast.parse` of each file at `origin/audit/simplification-plan` and at `d40aa0f` collected every `whitelist.spare`/`whitelist.set_override` call, normalized quote style and filled the `spare_days=0` / `now=None` defaults, and compared positionally. 41 calls per side (2+11+2+26), zero mismatches in `media_key`, `title`, `note`, `spare_days`, `now` or the positional `session`. The one reordered hunk (`TestPurgeExpiredSpares`, `radarr:1:9` appearing to move) is diff alignment; source order is unchanged. | `d40aa0f` |
| `src/reaper/services/whitelist.py:spare` — the-shim-did-more-than-set_override | The deleted `spare()` was a pure delegation with no body of its own: same keyword-only signature minus `decision`, same `spare_days: int = 0` and `now: datetime \| None = None` defaults, and a single `return await set_override(..., decision="spare", ...)` forwarding all five. Read at the base commit. `decision="spare"` at a call site reproduces it exactly; expiry, idempotence and the flush all live in `set_override` and always did. | `d40aa0f` |
| `src/reaper/api/whitelist.py:_sync_grace_clocks` — cleared_spare-arm-orphaned-with-unspare_item | The `cleared_spare` parameter and its clock-wipe branch (`:159-166`) are still reached: `clear_override` (`:239`) passes `cleared_spare=prior == "spare"`, and `test_override_truth.py:708-729` (`test_clearing_a_spare_restarts_a_clock_burned_down_while_invisible`) drives the True arm through `DELETE /api/override/{media_key}` with a seeded three-week-old clock, asserting the fresh window. Rule 4/71's B-2 defense keeps its only driver. | `d40aa0f` |
| `src/reaper/api/runs.py:121`, `.claude/rules/review-queue.md:79` — citation-names-a-deleted-whitelist-surface | Both cite `api/whitelist`'s grace-clock write, not a deleted route. `runs.py:121`'s rule-72 deferral ("refusing there would refuse a *spare*") still describes `POST /api/override` → `_sync_grace_clocks`; the rule file cites `_sync_grace_clocks` by name and the function survives unchanged. Neither names `list_whitelist`, `spare_item`, `unspare_item`, `SpareIn` or `list_spared`. | `d40aa0f` |
| `src/reaper/api/whitelist.py` — module-docstring-describes-a-retired-route | The docstring speaks of "these routes" without enumerating them, and every claim in it holds for the two survivors: spare/reap semantics, a show-level media_key covering every season, and "Nothing here deletes. Removing an override does not reap anything." No sentence describes a read surface or a `/whitelist` path. | `d40aa0f` |
| `docs/STATUS.md` — not-updated-for-an-external-contract-change | Deleting three routes alters what the app serves, but `STATUS.md` carries no line describing the keep-list route surface — grepped `whitelist`, `override`, `route` and `API` across the file, one unrelated milestone row. Nothing in it is now wrong, so the same-commit rule has nothing to edit. `docs/DECISIONS.md` holds no daggered section for it either. | `d40aa0f` |
| `src/reaper/engine/policy_migrations.py:convert_list_protections` — third-shim-skips-drop-retired-gate-keys | It is the one raw-dict shim that never calls `drop_retired_gate_keys`, but the body that would break needs both `keep_rating_rules` AND `secondary`, which migration `e6f708192a3b` rewrote; and if one occurred, the `ValidationError` fallback runs `rebalance`, which strips the key, so the body loads with the conversion intact. The cost is a spurious `RESCALED` notice, not a lost protection. | `393625f` |
| `tests/test_api_type_mirror.py:INNER_MODULES` — split-drops-a-type-from-the-wire-mirror | `INNER_MODULES` still names only `reaper.engine.policy` after `PolicyWarning` moved to `policy_warnings.py`, but the walk collects Pydantic models and the browser's `PolicyWarning` pairs with `api.schemas.PolicyWarningOut` on the suffix rule, which the wire layer wins. Re-running `_declarations`/`_pair` with both new modules added gives a byte-identical pair map. `PolicyRepair` is a `StrEnum` the walk never collected. | `393625f` |
| `src/reaper/engine/policy_migrations.py:recover_rating_rules` — schema-version-now-import-bound | `SCHEMA_VERSION` became a cross-module `from ... import`, so a rebind of `policy.SCHEMA_VERSION` would no longer reach it. Nothing rebinds it: only `SCORER_VERSION` is monkeypatched, and it stayed in `policy.py`. | `393625f` |
| `scripts/mutation_scope.py:ZONES` — zone-module-no-longer-holds-its-functions | All three policy zones repointed with the split, and each zone's `functions=` members are in the file its `module=` names. `PolicyBody._drop_retired_gates` sits under `policy-save-boundary`, which still names `policy.py`. | `393625f` |
| `src/reaper/clients/tautulli.py:library_media_info` — malformed-envelope-coerced-to-empty-page | Verified against Tautulli `master`: for `get_library_media_info` a `result: success` envelope with `data` null or non-dict cannot occur, so the `else {}` arm never fires. The builder coerces a `None` return to `{}` (`api2.py:_api_responds`), making `data: null` structurally impossible, and `get_datatables_media_info` returns a dict on every path (the populated `output` on success, a valid `default_return` `{recordsFiltered: 0, data: []}` on all eight failure paths). An unhandled exception yields `result: error` at HTTP 500, which `call()` already raises `IntegrationError` on. Even if the arm fired, an empty library reads every item unmatched, which abstains and keeps (the `library_index.py` short-page-drops-index-items row). The one adjacent shape that DOES reach Reaper, Tautulli serving its own internal failure as that same empty `default_return`, is a different mechanism (a valid dict, not null) and is also keep-safe: watch history comes from the separate `get_history` mirror keyed by rating_key, and the plexapi sweep re-adds the section's items as `fresh` with Plex's own `added_at`, so they are judged on real evidence and gain no delete pressure. Issue #621. | `aec992f` |
| `src/reaper/clients/seerr.py:all_requests,users` — paging-advances-by-the-constant-not-the-page | Verified against Overseerr and Jellyseerr `develop`: neither caps the `take` parameter on `GET /api/v1/request` or `GET /api/v1/user`. Each handler passes the client's `take` straight to TypeORM `.take(pageSize)` with no maximum (Overseerr `request.ts:32,151`, `user/index.ts:31,61`; Jellyseerr `request.ts:36,180`, `user/index.ts:43-45,148`), and `pageInfo.results` is the full count from `.getManyAndCount()`. So every non-final page returns exactly `take` rows, and `skip += DEFAULT_PAGE_SIZE` against `pageInfo.results` is complete. The only clamp-like construct is a `Math.max(10, includeIds.length)` on Jellyseerr's user default branch, used only when `take` is absent, which raises a floor and never caps. Promoted from the unverified carry-over below. The `len()`-based advance is still worth making on `plex.py`'s `_iter_pages` argument, but is not a correctness fix. Issue #615. | `aec992f` |
| `.github/workflows/ci.yml:changes` — prose-glob-misses-root-level-markdown | The claim was that `**/*.md` matches nested files only, so a root-level `README.md` or `CLAUDE.md` classified as code and ran the full lane set. `**` matches zero directories in this filter's syntax, so the glob does match root-level files, and four consecutive `dev` pushes touching only root markdown each ran `hygiene` alone, which is the prose verdict. Issue #590, closed `Reviewed/Invalid`. The neighboring observation is TRUE and is a different finding: `tests/test_repo_hygiene.py`'s CodeQL guard pins the three path lists by SPELLING rather than by semantics, so had this class been real the guard could not have caught it, only paired a fix to it. | `2ddadac` |

## Carried over with NO evidence — unverified, not refuted

These seven came forward from the frozen `docs/history/CODE_REVIEW.md` at `d3c3839` carrying a
verdict and no reason. A row that says "skip this" without saying why is worse than no row, so
**none of them binds**. A pass that lands on one of these re-derives it from the code and reports
what it finds; they are listed only so nobody re-imports them as settled.

| Fingerprint | The claim, unverified |
| --- | --- |
| `src/reaper/api/middleware.py` — api-key-lane-writes-profile | The API-key lane may write `/api/profile`, so a header-only credential can turn off the run caps interlock and lower the grace window. (The fence has been rewritten twice since — see the three evidenced `middleware.py` rows in the index above.) |
| `src/reaper/db/session.py` — synchronous-normal-journal-not-durable | `PRAGMA synchronous=NORMAL` makes the deletion journal non-durable across a host crash, contradicting the durability the `ActionStep` / `StepState` docstrings and rule 26 assert. |
| `src/reaper/engine/facts_codec.py:facts_from_dict` — raises-on-older-rows | Raises on any stored `facts_json` written before an `_OBS_FIELDS` entry or a `GateId` value existed, 500-ing the simulator instead of falling back to a fresh scan. |
| `src/reaper/engine/identity.py` — rationale-cites-unreachable-callers | The design rationale claims the single production join is reachable from the backtest and the planner; neither module references identity at all. |
| `src/reaper/engine/policy.py:PolicyBody.gates` — missing-gate-silently-not-run | A gate missing from `PolicyBody.gates` is silently not run and cannot be warned about; an empty gates tuple validates and removes every built-in protection. |
| `src/reaper/services/instances.py` — stored-key-shipped-to-any-host | A stored instance API key can be shipped to any host by editing `base_url` and pressing Test, defeating the module's stated write-only invariant. |
| `src/reaper/services/season_pruning.py` — group-key-unindexed | Every spare/override click full-scans the never-garbage-collected candidate table because `group_key` is unindexed. |

## Overturned — refutations later proven wrong

Kept as the record of the miss, per the file's own standard. **None of these binds.** A pass that
reaches one of these candidates again treats it as live.

**The mid-binge guard — refuted at `65359be`, real, issue #95.** The refutation said
`sequential_protections` reads per-user positions rather than a watcher count and is already
time-bounded by `in_progress_hold_days`. Driven through `active_progress` and `plan_series_prune`,
one viewer who finished a season 120 days ago is protected under a 400-day mirror and prunable
under a 90-day one. Both halves of the refutation were true statements that did not support the
conclusion: rule 140 binds readers of a re-qualified VALUE, not readers of one named field, and
`in_progress_hold_days` is the span the guard CLAIMS to cover, not a bound on the evidence. The
word in the original row was "declined", never "refuted", which is why it should never have been
filed here.

**`src/reaper/engine/policy_warnings.py:inspect` and the FEW_WATCHERS signal — refuted at `f8592b3`,
wrong, issue #164.** The refutation measured "the signal alone shifts the line, it does not empty
the list" at the shipped weights only, where `few_watchers = 20` puts coverage at 8000bp against
the 5000 floor. Weights need only total 100, so `unwatched 40 / few_watchers 60` is legal and
gives 4000bp: every item abstains, library-wide, and `inspect` says nothing. **Re-measure a
weight-dependent claim across the range the operator can set, not at the default.**

**The condemn lane emptying the list — refuted at `94f11fc`, wrong, issue #164.** The refutation
named coverage in its own reasoning and then did not follow it. A blocked signal leaves the
numerator while its weight stays in the denominator, so enough weight on reach-bounded fields
drops coverage under the floor and `decide_verdict` abstains on exactly the second arm the
sentence itself lists. Measured with the gate off, a 60-day floor and a 90-day reach:
`unwatched 40 / few_watchers 60` gives coverage 0.40 against 0.50. "Nothing sets `blocked`" was
true and never sufficient.

## PR 208 — the CONFLICTED split and the retired spine rows (refuted at `bbfe930`)

**`services/library_index.py:build_index` — retirement breaks a merged bind.** A retired row is
by definition unenriched, so it carries no ids, no basename and no `files`; it can never enter
tier 1, tier 2, or a `_narrow_among_id_hits` twin group. Retirement can only ever remove a tier-3
title+year candidate.

**`services/library_index.py:build_index` — retirement corrupts `fresh`, shifts `added_at`, or
shrinks watch history.** Retired keys are absent from `plex_items` by definition, so they cannot
re-enter through `fresh`; every surviving row still takes `added_at` from its own spine row; and
`_watch_stats` keys on `{i.plex_rating_key for i in items}`, never on the index. The `PlexIndex`
is consumed only by `resolve_movie`/`resolve_show` and two log-only helpers, so no gate, signal,
planner or executor path reads it.

**`services/library_index.py:_sweep` — `swept` true on a failed sweep.** Both the `plex is None`
and the `except PlexError` arms return `False` explicitly, and both are pinned by tests.

**`engine/identity.py:Resolution.candidate_rating_keys` — reaches a verdict.** Writers are
`_explain`; readers are `_replayed_match`, `deep_links.build_links` and `WhyPanel.MatchCandidates`.
Zero occurrences in `verdict.py`, `gates.py`, `signals.py`, `planner.py`, `executor.py`.
`fairness._match_candidates` is an unrelated symbol (Seerr request binding).

**`engine/identity.py:resolve` — an abstain mislabeled as conflicted.** Only four non-bind sites
exist; the two multiplicity cases keep `abstain`, the two disagreement cases take `conflicted`,
and all four pass `candidates`. `Resolution.unmatched()` correctly carries none.

**`api/review.py:_primary_reason` / `_chip` — the defaulted `media_type` names the wrong app.**
The only production call sites are both in `_candidate_out` and both pass `r.media_type`. Every
other caller is a test, and `Candidate.media_type` is only ever `"movie"` or `"season"`, so
`_managing_app` is total.

**`api/schemas.py:LinksOut` / `api.ts:Links` — shape disagreement across the wire.** Pydantic
always serializes `candidate_rating_keys` and `match_candidates`, so the TS optionals are
supersets; the `[]` default is deep-copied, so no shared-mutable bug. A record stored before the
field shipped reads `None` -> `()` -> `[]` -> the component returns null. No crash, no wrong
default.

**`WhyPanel.tsx:MatchCandidates` — "Plex 1" and "Tautulli 1" can stop pairing the same row.**
The number comes from the candidate array index, not the rendered pill position, and both link
builders read *item-wide* coordinates, so a missing app blanks that app for every candidate at
once. The hypothesized "Plex 1, Plex 2, Tautulli 2" cannot occur.

**`WhyPanel.tsx:MatchCandidates` — invalid DOM from `<span display:flex>` inside `<Notice>`.**
`Notice` defaults to `as="p"`; `<span>`/`<a>` are phrasing content and legal in `<p>`, and React
constructs the DOM rather than parsing it. `.candidate-group`'s `inline-flex` is an atomic box
that cannot split across a wrap, so the CSS comment's pairing claim is implemented.

**`index.css:.match-candidates-lead` — an unstyled class.** It inherits `color`/`font-size` from
`.match-candidates`. An unstyled hook is not a defect.

**`api.ts:Match.candidate_rating_keys` — declared and never read.** Documented in place as an
audit field for whoever reads the response; rule 64 governs removing a surface, not adding an
unused wire field.

**`routes.py:_deep_links` — "the links and the replay can never disagree".** Implemented: both
readers are literally `_replayed_match(row)`, one derivation, rule 104 satisfied. The unreadable
branch omits the key and `_deep_links` uses `.get(..., ())`.

**`PolicyEditor.tsx:WarningAnchor.guard` — a single `WarningGuard` cannot express a `WarnBlock`
nested under two mount conditions (issue #200).** The premise is false: `keep_rating_rules` is
already that anchor. Its block at `:529` sits inside `{gate.enabled && (` at `:478`, and the
whole `RatingFloorRow` is conditionally mounted by the IIFE at `:1587-1590`. It is expressible
because `:957` folds both into one boolean — `some((g) => g.gate === "rating_floor" &&
g.enabled)`. A conjunction fits one guard, so the type limit does not bind and the fold is the
idiom. Widening `guard` to a set and composing the walk's page states buys nothing over it.

**`executor.py:_grew_materially` — the season growth interlock compares a frozen Sonarr season
*folder* against a live sum of episode *files*, so it is desensitized (issue #263).** Refuted
twice over, at the service rather than in the tree. **Upstream:** Sonarr's
`SeriesStatisticsRepository.EpisodeFilesBuilder()` is `SUM(COALESCE("Size", 0)) AS SizeOnDisk`
`FROM "EpisodeFiles"`, grouped by series and season, on `v4.0.18.2978` and unchanged in shape
across every stable tag from v3 to `v4.0.19.2995`; Radarr's `MovieStatisticsRepository` is the
same over `MovieFiles`. In neither tree does `IDiskProvider.GetFolderSize` have a production
call site at all — its only reference outside the declaration is a test asserting it is never
called. **Measured:** every season on two live Sonarr `4.0.18.2978` instances, read both ways,
**every one of the several thousand matched to the byte**, ratio exactly 1.000000. So the frozen and live halves
are one quantity and the blind band is only the tolerance `_grew_materially` declares. Pinned to
`f6ddf24`. Note the *shape* of the error rather than the conclusion: five sites agreed with each
other because each was written from one of the others, and the claim was never checked against
Sonarr. See learning 14.

**`StaleReadNotice.tsx` — 18 surfaces announce a failed background refetch nobody asked for, so
the shared component should be `standing` (issue #392).** The premise is false on all but two of
the call sites, and it was written from the component's *mount condition* without reading what
refetches the queries behind it. `main.tsx:17` sets `refetchOnWindowFocus: false` app-wide with
`staleTime: 30_000`, so a read here refetches only when it mounts or when a mutation invalidates
its key. On 21 of the 23 call sites that makes the stale line a REACTION -- the operator saved a
connection, switched a Plex server, removed a webhook -- and `role="alert"` is what those call
sites should carry. The issue also claimed a window-focus refetch reaches it; there is no such
refetch anywhere in the app. Two callers genuinely arrive unprompted: the Security panel's read is
`useSafety`, whose `refetchInterval` is 15s and always on, and the Jobs panel's `["schedule"]`
polls at 1500ms while any job runs, which a scheduled job satisfies. Neither is worth a
caller-level opt-out on a component whose whole point is that the wording cannot drift: the
repetition they can produce is already bounded by `collapseStaleReads` (#198), which folds a
panel's simultaneous failures into one line for exactly this reason and reasons about the
announcements out loud. Note the shape of the error rather than the conclusion: "no press is on
this path" is a claim about a QUERY's refetch policy, not about a component's props, and reading
only the latter made 21 reactions look like furniture.

**`engine/preview.py:_bare_facts` — the docstring promises `Unknown` and six fields default to
`Absent`, so a future keep probe would understate an operator's protection.** The literal count
is right and the consequence is unreachable. `_bare_facts` sets 13 of `Facts`' 20 fields (11
`Unknown`, `title` a str, `history_reach_days` a disclosed `Known(36_500)`); six fall through to
`_UNSET = Absent(source="unset")` (`gates.py:59`) and `ratings` to `()`. But the harm named
cannot occur: `evaluate_keep` (`signals.py:626-673`) reads exactly ONE fact, `spec.read(facts)`
for its own configured field, and any probe sets that field `Known` at `preview.py:85` before
asking — so the `Absent`→0.0 / `Unknown`→full-discount asymmetry can never touch the probed
field. The one cross-field reader, `fields.reach_shortfall`, points the OPPOSITE way: on a
non-`Known` `days_since_added` it reports a shortfall, which `evaluate_keep` answers with the
FULL discount, i.e. more protection shown, not less. On the path that exists, `_branch_signal`
reads only the six fields that ARE enumerated, so the docstring's operative claim — "nothing
else can move the answer" — is true as implemented. Pinned at `aba0c95`. The residual was taken:
the sentence now says which fields it covers, and names what a keep arm must set. Note the shape
rather than the conclusion: a literally-false count in a comment is not automatically a defect,
and the test is whether the clause the code RELIES on is the false one.

**`WhyPanel.tsx:SignalRow` — the ramp sentence is suppressed only for `unreadable`, so a
`not_applicable` row describes arithmetic that never ran.** Every mechanical claim is correct and
the defect is not. Verified by running the engine: an `Absent` `imdb_rating_tenths` returns
`not_applicable, pressure 0.0, floor 0, saturate_at 60`, `rowRampSentence` self-bails only on a
null bound, and the rendered string is byte-identical to a well-rated title's. It still misleads
nobody, because the two never appear together: a `not_applicable` row renders alone inside a
collapsed `<details>` headed "N didn't apply here" (`WhyPanel.tsx:977-986`) beside a detail line
reading "no IMDb rating", where a well-rated row lands in the "Argued to keep" group. No clause
is false about the item either — the ramp half describes the RULE, and "This one added 0" agrees
with the row's own 0 column. The `unreadable` exclusion has a state-specific reason the candidate
never accounted for: that row's column prints "Couldn't check" instead of a number, so "added 0"
would contradict it and assert the very zero the panel refuses to state. The one genuinely
misleading variant (`few_watchers` reading "all 20 points at 0 watchers" beside "no watch history
recorded") is unreachable: `distinct_watchers` is only ever `Known` or `Unknown`, never `Absent`.
Pinned at `aba0c95`. The residual was taken: the guard's comment now states its real reason and
says why `not_applicable` keeps the sentence.

**`clients/base.py:_trace` — `status=None` cannot distinguish a guard refusal from a timeout, so
a blocked DELETE reads as one that may have landed.** The mechanism is real and the conclusion is
not. `SafetyViolationError` is a `RuntimeError` (base.py:120) and matches neither `except` in
`_mutate`, so the `finally` does log `mutation=True status=None`. But the operator is never left
holding that ambiguity: the only three `_mutate` call sites reach it from inside the executor's
try at `executor.py:2012`, whose `except SafetyViolationError` records the step FAILED with "the
transport guard blocked the delete" and logs `reap.item_failed` at WARNING, where a timeout takes
the `IntegrationError` arm one line up. The disambiguating line is louder than the DEBUG trace,
not absent. The trigger is also a can't-happen tripwire by construction: the execute route hands
ONE `RuntimeSafety` snapshot to both the guard and the executor. Do not "fix" this by narrowing
what escapes `_mutate` — letting it pass both arms is what lets the executor turn it into a clean
failed item. Pinned at `e4bfa18`. One residual half was real and taken separately: `_send`
assigns `status` once outside its redirect loop, so a 302 followed by a failing hop traced a
stale `status=302`; that is cosmetic, unreachable for mutations (`_mutate` refuses redirects),
and needs three consecutive transport failures behind `@transient_retry` to reach at all.

**`api/auth.py:recover` — the lockout sweep missed the recovery endpoint, leaving the only
lockout counter in the tree that crosses silently.** The premise is false. `recover_throttle.
record_failure(ip_key)` is reachable only inside `if not await redeem_recovery_token(...)`, and
all three of that function's False returns already log at WARNING first: `recovery.rejected` with
"unknown token", "token already used", and "token expired" (`auth/recovery.py:242/245/248`). A
token-guessing flood therefore emits a WARNING per attempt before the counter is touched, which
is louder than the proposed fix, and `/api/auth/recover` also carries a DEBUG `http.request` line
per attempt. The census behind the candidate was miscounted too: five `record_failure` sites, of
which four warn. Routing it through `record_password_failure` would emit `auth.password_rejected`
on a lane that has no password. Pinned at `e4bfa18`. Noted but not raised: `_rate_limited` /
`RateLimiter` (`ratelimit.py:154`) logs nothing on refusal, which is a closer sibling than
recovery is if anyone wants to sweep this class properly.

**`services/planner.py:build_plan` — `planner.admitted` is decided against `planned_keys`, which
predates the step-expansion loop, so an item can be logged admitted and skipped in one run.** The
divergence is real in the abstract and unreachable in production. Reproducing it needs a
hand-built Candidate with a three-part `sonarr:{inst}:{series}` media_key, which nothing writes:
`Candidate` is constructed in one place (`snapshot.py:1463`) from either the movie path
(`radarr:{inst}:{id}`) or `season_scan.season_media_key`, which always emits four parts. The
three-part string exists only as a `group_key` and as a display-lookup key, and
`effective_condemned` only ever returns rows keyed by `media_key`. The `else` branch at
`planner.py:593` is defensive code for a series-delete path that does not exist. The proposed fix
was also a no-op: both DEBUG loops are ALREADY below the step-expansion loop. Recomputing
`planned_keys` after it is NOT a safe local edit — the same set feeds `admitted`/`omitted`, the
abort-not-truncate refusal, and `held_back_unknown_size`, all decided before the ReapRun row is
created. Pinned at `e4bfa18`.

**`tests/test_override_truth.py:_overrides` — the `since(0)` length baseline is defeated by ring
saturation, so the negative test passes while the forbidden line was emitted.** Refuted AS
STATED: the stated repro does not reproduce. With the no-op DELETE made to log anyway, the test
went red running alone, running the candidate's own saturation recipe, and under a full
`-n auto` suite (1 failed, 3432 passed). The DELETE moves the window by exactly one line, so the
baseline is only defeated when an override line occupies the window's single oldest slot as it is
sampled — swept at pad=0/1/5/60/300/495/498 (caught) versus pad=499 (missed), one offset in the
sweep. Pinned at `e4bfa18`. The narrower real defect was taken: the helper now takes a
`last_seq()` cursor, since a one-line race that can allow a forbidden line is still worth closing
and the three positive tests carried it as a false failure.

**`services/season_scan.py:season_watch_stats` — the two `continue`s that drop a viewer's
progress row leave the mid-binge hold unfired, so a season somebody is half way through reads as
Absent (#470).** Traced end to end; the season comes out protected, both branches. The finding
read one query and the control is in another: `user_season_progress` comes from the `progress`
read (which filters `media_index IS NOT NULL`, then drops these two cases), but
`_progress_by_user` **anchors** on `user_season_keys`, which comes from the `pairs` read — no
`media_index` filter at all, so `pairs` is a strict superset on `(user, key)`. A viewer whose
position was dropped is therefore still present as a touch, records as `None` ("position unknown"
rather than absent), and `_anchor_positions` fails closed on `watched is None`, holding the anchor
season and the one after it. Both `max_ep is None` and `max_unknown > max_ep` produce
`{'7': {3: None}}` → `prunable [1, 2]`, seasons 3 and 4 held. Pinned at `eb1b60b`. What the pass
was right about is that nothing pinned the invariant: it lives in a query neither branch sits
next to, and narrowing `pairs` to match `progress` would delete the protection while reading as a
tidy-up. Now asserted end to end in
`test_season_scan.py::test_a_dropped_position_holds_the_season_rather_than_clearing_it` (#485),
which goes red under exactly that mutation.
