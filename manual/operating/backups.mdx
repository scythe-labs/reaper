---
id: backups
title: Back this up
description: Two files in the data volume cannot be regenerated. A third is disposable by design.
sidebar_position: 1
---

# Back this up

The `/data` volume holds two things you cannot regenerate.

| File | Why it matters |
| --- | --- |
| `reaper.db` | Your policies, decisions, whitelist and audit trail. |
| `secret.key` | The **only** thing that can decrypt your stored API keys. |

<Callout tone="caution">
  **Treat `reaper.db` as sensitive.** It holds the credentials for every service Reaper talks
  to, including a Plex token that grants full control of your Plex account. Encrypted at rest is
  not the same as safe in a public issue report.
</Callout>

## What you do not need to back up

`cache.db` sits next to them and is disposable by design. It is other people's data mirrored
locally, meaning watch history and the IMDb dataset, and Reaper rebuilds it.

Separating them states the invariant out loud: nothing in `cache.db` is a source of truth.

## Restoring

Put both files back in the data volume and start the container. Migrations run before the app
accepts a connection, so a database from an older version is brought forward on the way up.

If you restore `reaper.db` without its matching `secret.key`, every stored credential is
unreadable and must be entered again. The policies, decisions and audit trail survive.
