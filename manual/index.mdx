---
id: index
title: Reaper
description: Explainable media library pruning for Plex. Find what nobody watches, see why, remove it safely.
sidebar_position: 0
slug: /
---

# Reaper

Reaper finds the movies and shows nobody watches, explains why it thinks each one is
expendable, and removes them safely through Sonarr and Radarr.

<Callout tone="tip">
  **It ships read-only.** A new install can scan, score, and explain, and nothing else, until
  you arm deletion with a password.
</Callout>

## Start here

- **[What Reaper does](/getting-started/overview)** if you are still deciding.
- **[Install](/getting-started/install)** to get a container running.
- **[First run](/getting-started/first-run)** for the order to do things in.
- **[Understanding policy](/policy/understanding-policy)** once it is scanning and you want to
  tune it.

## The thing it does that others do not

Reaper explains the keeps, not only the deletes. Every candidate carries the signals that
counted against it **and** every protection that was checked and did not fire, with the real
numbers behind each one.

Two independent layers sit under every deletion, and neither is trusted alone. Deletion is off
until you turn it on, the refusal lives at the network layer rather than in scattered checks,
and a missing rating or an unreachable service can only ever protect a file.

[How a delete is kept safe](/safety/how-a-delete-is-kept-safe) is the page that walks it
through.
