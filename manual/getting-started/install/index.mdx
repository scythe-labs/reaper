---
id: install
title: Install
description: One container, no access to your media, and one small volume. Pick your route.
sidebar_position: 1
---

# Install

Reaper ships as **one container**, and it needs no access to your media. It has no filesystem
delete path: only Sonarr and Radarr do. The only volume is its own small database.

<Callout tone="caution">
  **If you find yourself mounting your library, something is wrong.** Reaper never needs to see
  your media files.
</Callout>

## Pick your route

- **[Docker](/getting-started/install/docker)**, which is a single `docker compose up -d`.
- **[Unraid](/getting-started/install/unraid)**, using the community template.

Both land in the same place. Follow [First run](/getting-started/first-run) afterwards.

## File ownership

<Callout tone="note">
  **Ownership is handled for you.** The container starts as root only to take ownership of its
  data folder, then drops to an unprivileged user before it opens the database. A bind mount
  created owned by root just works.
</Callout>

Set `PUID` and `PGID` to choose that user. The default is `1000`. The app process itself never
runs as root.

To keep it off root entirely, pin `user: "1000:1000"` and create the folder owned by 1000
yourself.

## What to back up

Three files in the data volume, covered in [Back this up](/operating/backups): `reaper.db`,
`secret.key` and `secret.salt`. The key and the salt are useless apart. Set that up before you
arm anything.
